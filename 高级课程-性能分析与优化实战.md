# LevelDB性能分析与优化实战

## 🎯 课程目标

本课程将教你成为性能优化专家，掌握：
- 使用perf/VTune/火焰图进行CPU性能分析
- 识别和优化热点代码路径
- 理解CPU缓存、分支预测、SIMD
- 阅读汇编代码，理解编译器优化
- 使用各种profiling工具定位瓶颈

---

## 目录

1. [性能分析工具](#1-性能分析工具)
2. [CPU性能优化](#2-cpu性能优化)
3. [内存性能优化](#3-内存性能优化)
4. [编译器优化](#4-编译器优化)
5. [汇编代码阅读](#5-汇编代码阅读)
6. [SIMD优化](#6-simd优化)
7. [实战案例](#7-实战案例)

---

## 1. 性能分析工具

### 1.1 perf - Linux性能分析利器

```bash
# 安装perf
sudo apt-get install linux-tools-common linux-tools-generic

# 基本用法

# 1. 统计概览（快速判断瓶颈）
perf stat ./db_bench --benchmarks=fillrandom --num=1000000

# 输出示例：
# Performance counter stats for './db_bench':
#
#     42,589.31 msec task-clock                #    0.998 CPUs utilized
#           156      context-switches          #    3.664 K/sec
#             2      cpu-migrations            #    0.047 K/sec
#         3,142      page-faults               #   73.762 K/sec
# 156,234,567,890  cycles                      #    3.669 GHz
# 123,456,789,012  instructions                #    0.79  insn per cycle  ← 关键指标
#  23,456,789,012  branches                    #  550.789 M/sec
#     345,678,901  branch-misses               #    1.47% of all branches ← 分支预测失败率
#  45,678,901,234  L1-dcache-loads             # 1072.456 M/sec
#   1,234,567,890  L1-dcache-load-misses       #    2.70% of all L1-dcache hits ← Cache命中率
#     123,456,789  LLC-loads                   #    2.899 M/sec
#      12,345,678  LLC-load-misses             #   10.01% of all LL-cache hits

# 关键指标解读：
# - IPC (insn per cycle): 0.79 ← 每个时钟周期执行0.79条指令（理想是>2）
# - Branch misses: 1.47% ← 分支预测失败（理想<1%）
# - L1 cache misses: 2.70% ← L1缓存缺失（理想<5%）
# - LLC misses: 10.01% ← 三级缓存缺失（影响性能）

# 2. 记录性能数据
perf record -g ./db_bench --benchmarks=fillrandom --num=100000
# -g: 记录调用栈

# 3. 查看报告
perf report
# 输出热点函数列表（按CPU占用排序）

# 4. 查看具体函数的汇编代码
perf annotate leveldb::MemTable::Add

# 5. 生成火焰图
perf script | ~/FlameGraph/stackcollapse-perf.pl | ~/FlameGraph/flamegraph.pl > flamegraph.svg
```

### 1.2 实战：分析SkipList::Insert性能

```bash
# 步骤1：编译优化版本
cd /home/dev/leveldb
mkdir -p build-release && cd build-release
cmake -DCMAKE_BUILD_TYPE=Release -DCMAKE_CXX_FLAGS="-g -O2 -fno-omit-frame-pointer" ..
cmake --build .

# 步骤2：运行并采样
perf record -F 999 -g ./db_bench --benchmarks=fillrandom --num=1000000

# 步骤3：查看热点
perf report --stdio | head -50

# 预期输出（热点函数）：
# Samples: 42K of event 'cycles'
# Event count (approx.): 38540234561
#
# Overhead  Command   Shared Object      Symbol
# ........  ........  .................  .................................
#
#    15.23%  db_bench  db_bench           [.] leveldb::SkipList<...>::Insert
#    12.45%  db_bench  db_bench           [.] leveldb::MemTable::Add
#     8.67%  db_bench  db_bench           [.] leveldb::InternalKeyComparator::Compare
#     7.89%  db_bench  db_bench           [.] leveldb::SkipList<...>::FindGreaterOrEqual
#     6.54%  db_bench  db_bench           [.] std::_Rb_tree_insert_and_rebalance  ← 意外！
#     5.32%  db_bench  db_bench           [.] memcpy
#     4.21%  db_bench  [kernel]           [k] copy_user_enhanced_fast_string

# 步骤4：查看函数汇编（找优化点）
perf annotate -s leveldb::SkipList::Insert
```

### 1.3 火焰图分析

```bash
# 生成火焰图
git clone https://github.com/brendangregg/FlameGraph
cd /home/dev/leveldb/build-release

perf record -F 99 -a -g -- ./db_bench --benchmarks=fillrandom --num=1000000
perf script | ~/FlameGraph/stackcollapse-perf.pl | ~/FlameGraph/flamegraph.pl > fillrandom_flame.svg

# 在浏览器中打开 fillrandom_flame.svg
```

**火焰图阅读技巧：**
```
火焰图结构：
┌─────────────────────────────────────────┐
│ 最顶层：叶子函数（实际执行代码）          │  ← 优化重点
├─────────────────────────────────────────┤
│ 中间层：调用路径                         │
├─────────────────────────────────────────┤
│ 底层：程序入口（main等）                 │
└─────────────────────────────────────────┘

颜色：
- 红色系：用户态CPU
- 蓝色系：内核态CPU
- 绿色系：库函数

宽度 = CPU占用时间

分析方法：
1. 找最宽的"平台"（占用最多CPU的函数）
2. 找最高的"塔"（调用栈最深，可能有优化空间）
3. 关注意外的宽块（不应该出现的热点）
```

### 1.4 自定义性能计数器

```cpp
// perf_counter.h - 自定义性能计数器
#include <chrono>
#include <string>
#include <unordered_map>
#include <iostream>

class PerfCounter {
 public:
  static PerfCounter& Instance() {
    static PerfCounter instance;
    return instance;
  }

  class Timer {
   public:
    Timer(const std::string& name) : name_(name) {
      start_ = std::chrono::high_resolution_clock::now();
    }

    ~Timer() {
      auto end = std::chrono::high_resolution_clock::now();
      auto duration = std::chrono::duration_cast<std::chrono::nanoseconds>(
          end - start_);
      PerfCounter::Instance().Add(name_, duration.count());
    }

   private:
    std::string name_;
    std::chrono::time_point<std::chrono::high_resolution_clock> start_;
  };

  void Add(const std::string& name, long long ns) {
    counters_[name].count++;
    counters_[name].total_ns += ns;
  }

  void Report() {
    std::cout << "\n=== Performance Report ===\n";

    // 按总时间排序
    std::vector<std::pair<std::string, Stats>> sorted;
    for (const auto& kv : counters_) {
      sorted.push_back(kv);
    }
    std::sort(sorted.begin(), sorted.end(),
              [](const auto& a, const auto& b) {
                return a.second.total_ns > b.second.total_ns;
              });

    for (const auto& kv : sorted) {
      const std::string& name = kv.first;
      const Stats& stats = kv.second;

      double avg_us = (double)stats.total_ns / stats.count / 1000.0;
      double total_ms = (double)stats.total_ns / 1000000.0;

      std::cout << name << ":\n";
      std::cout << "  Count: " << stats.count << "\n";
      std::cout << "  Total: " << total_ms << " ms\n";
      std::cout << "  Avg: " << avg_us << " us\n";
    }
  }

 private:
  struct Stats {
    long long count = 0;
    long long total_ns = 0;
  };

  std::unordered_map<std::string, Stats> counters_;
};

// 使用示例
void OptimizedMemTableAdd(const Slice& key, const Slice& value) {
  PerfCounter::Timer timer("MemTable::Add");

  {
    PerfCounter::Timer t1("MemTable::Add::EncodeKey");
    // 编码key...
  }

  {
    PerfCounter::Timer t2("MemTable::Add::SkipListInsert");
    // 插入SkipList...
  }
}

// 程序结束时打印报告
void Cleanup() {
  PerfCounter::Instance().Report();
}

// 输出示例：
// === Performance Report ===
// MemTable::Add:
//   Count: 1000000
//   Total: 2534.5 ms
//   Avg: 2.53 us
// MemTable::Add::SkipListInsert:
//   Count: 1000000
//   Total: 1823.2 ms (占72%)
//   Avg: 1.82 us
// MemTable::Add::EncodeKey:
//   Count: 1000000
//   Total: 711.3 ms (占28%)
//   Avg: 0.71 us
```

---

## 2. CPU性能优化

### 2.1 CPU缓存优化

```cpp
// 理解CPU缓存层次

/*
现代CPU缓存层次（Intel i7为例）：

L1 Data Cache: 32KB per core
  - Latency: 4 cycles (~1ns)
  - Bandwidth: ~200 GB/s

L2 Cache: 256KB per core
  - Latency: 12 cycles (~3ns)
  - Bandwidth: ~100 GB/s

L3 Cache (LLC): 8MB shared
  - Latency: 40 cycles (~10ns)
  - Bandwidth: ~50 GB/s

Main Memory (RAM):
  - Latency: 200+ cycles (~60ns)
  - Bandwidth: ~20 GB/s

关键洞察：
- L1命中 vs 内存访问：60倍延迟差距
- Cache line大小：64 bytes
- 顺序访问 vs 随机访问：10-100倍性能差距
*/

// 案例1：缓存友好的遍历
class CacheFriendlyIteration {
 public:
  // 错误：列优先访问（Cache unfriendly）
  void BadIteration() {
    const int N = 1024;
    int matrix[N][N];

    for (int col = 0; col < N; col++) {
      for (int row = 0; row < N; row++) {
        matrix[row][col] = 0;  // 每次访问跳过N个元素
                               // 每个Cache line只用了1/16
      }
    }
    // 性能：~50 ms
  }

  // 正确：行优先访问（Cache friendly）
  void GoodIteration() {
    const int N = 1024;
    int matrix[N][N];

    for (int row = 0; row < N; row++) {
      for (int col = 0; col < N; col++) {
        matrix[row][col] = 0;  // 连续访问，充分利用Cache line
      }
    }
    // 性能：~5 ms（10倍提升！）
  }
};

// 案例2：数据对齐优化
class CacheLineAlignment {
 public:
  // 错误：虚假共享（False Sharing）
  struct BadCounters {
    int counter1;  // 假设在cache line 0
    int counter2;  // 同一个cache line
  };
  // 多线程修改不同计数器，但会导致cache line在CPU间频繁传输

  // 正确：对齐到不同cache line
  struct GoodCounters {
    alignas(64) int counter1;  // 独占cache line 0
    alignas(64) int counter2;  // 独占cache line 1
  };
  // 多线程修改，无cache line竞争
};

// LevelDB中的实际应用
class SkipListCacheOptimization {
 public:
  // SkipList节点设计
  struct Node {
    const Key key;  // 常量，只读，Cache友好

    // next指针数组：柔性数组，连续内存
    std::atomic<Node*> next_[1];
    // 优势：
    // - 所有next指针在同一cache line
    // - 遍历时预取效果好
  };

  // Arena分配：所有节点连续分配
  // 优势：
  // - 时间局部性：刚创建的节点马上使用
  // - 空间局部性：相邻节点可能在同一cache line
};

// 测试工具：测量cache miss率
void MeasureCacheMisses() {
  // 使用perf
  system("perf stat -e L1-dcache-loads,L1-dcache-load-misses,"
         "LLC-loads,LLC-load-misses ./db_bench --benchmarks=fillrandom --num=100000");

  // 优化前：
  // L1-dcache-load-misses: 5.2%
  // LLC-load-misses: 12.5%

  // 优化后（数据对齐+顺序访问）：
  // L1-dcache-load-misses: 2.1% (2.5倍改进)
  // LLC-load-misses: 4.3% (3倍改进)
}
```

### 2.2 分支预测优化

```cpp
// 现代CPU使用分支预测器猜测分支走向
// 预测错误会导致流水线清空（~15-20 cycles惩罚）

class BranchPredictionOptimization {
 public:
  // 案例1：去除分支

  // 错误：包含分支
  int AbsWithBranch(int x) {
    if (x < 0) {    // 分支，50%预测失败
      return -x;
    }
    return x;
  }

  // 正确：无分支版本
  int AbsWithoutBranch(int x) {
    int mask = x >> 31;  // 算术右移，负数为-1，非负为0
    return (x + mask) ^ mask;
    // 无分支，始终执行相同指令数
  }

  // 性能对比（100万次调用）：
  // With branch: 15 ms
  // Without branch: 5 ms (3倍提升！)


  // 案例2：likely/unlikely提示

  // 使用编译器提示优化分支预测
  #define likely(x)   __builtin_expect(!!(x), 1)
  #define unlikely(x) __builtin_expect(!!(x), 0)

  Status Get(const Slice& key, std::string* value) {
    Status s = mem_->Get(key, value);

    // 提示编译器：大多数情况下会命中
    if (likely(s.ok())) {
      return s;
    }

    // 提示编译器：错误路径不常见
    if (unlikely(s.IsCorruption())) {
      // 错误处理（冷路径）
      LogError(s);
    }

    return s;
  }


  // 案例3：LevelDB中的实际应用

  // db/skiplist.h - FindGreaterOrEqual
  Node* FindGreaterOrEqual(const Key& key) const {
    Node* x = head_;
    int level = GetMaxHeight() - 1;

    while (true) {
      Node* next = x->Next(level);

      // 关键优化：将常见情况放在前面
      if (next != nullptr && compare_(next->key, key) < 0) {
        x = next;  // 常见路径：继续向右
      } else {
        // 不常见路径：下降
        if (level == 0) {
          return next;
        } else {
          level--;
        }
      }
    }
  }
  // 优势：
  // - 常见路径（向右移动）的分支预测成功率高
  // - 降层操作较少，预测失败惩罚小
};

// 测试工具：测量分支预测失败率
void MeasureBranchMisses() {
  system("perf stat -e branches,branch-misses ./db_bench --benchmarks=readrandom --num=100000");

  // 优化前：
  // branch-misses: 3.2% of all branches

  // 优化后（使用likely/unlikely + 调整分支顺序）：
  // branch-misses: 1.1% of all branches (3倍改进)
}
```

### 2.3 循环优化

```cpp
class LoopOptimization {
 public:
  // 案例1：循环展开

  // 原始代码
  void SumNormal(const int* arr, int n) {
    int sum = 0;
    for (int i = 0; i < n; i++) {
      sum += arr[i];  // 每次迭代：1次加载，1次加法，1次跳转
    }
  }

  // 循环展开（编译器自动，也可手动）
  void SumUnrolled(const int* arr, int n) {
    int sum = 0;
    int i = 0;

    // 展开因子：4
    for (; i + 4 <= n; i += 4) {
      sum += arr[i];      // 1次跳转处理4个元素
      sum += arr[i + 1];  // 减少75%的跳转指令
      sum += arr[i + 2];
      sum += arr[i + 3];
    }

    // 处理剩余元素
    for (; i < n; i++) {
      sum += arr[i];
    }
  }
  // 性能提升：20-30%


  // 案例2：循环不变量外提

  // 错误：每次迭代都计算相同值
  void ProcessBad(const std::vector<int>& data, int multiplier) {
    for (size_t i = 0; i < data.size(); i++) {
      int scaled = data[i] * multiplier;
      int adjusted = scaled + data.size();  // data.size()是循环不变量
      // ...
    }
  }

  // 正确：提取到循环外
  void ProcessGood(const std::vector<int>& data, int multiplier) {
    size_t n = data.size();  // 提取到外面
    for (size_t i = 0; i < n; i++) {
      int scaled = data[i] * multiplier;
      int adjusted = scaled + n;  // 直接使用
      // ...
    }
  }


  // 案例3：LevelDB中的实际应用

  // table/block.cc - 二分查找优化
  Iterator* Seek(const Slice& target) {
    uint32_t left = 0;
    uint32_t right = NumRestarts() - 1;

    // 提取不变量
    const uint32_t num_restarts = NumRestarts();
    const char* data_base = data_;

    while (left < right) {
      uint32_t mid = (left + right + 1) / 2;

      // 使用局部变量，编译器优化更好
      uint32_t region_offset = GetRestartPoint(mid);
      const char* key_ptr = data_base + region_offset;

      // ... 比较 ...
    }
  }
};

// 编译器优化标志
void CompilerOptimizationFlags() {
  std::cout << "推荐的编译优化标志：\n\n";

  std::cout << "基础优化：\n";
  std::cout << "  -O2: 开启大多数优化（推荐）\n";
  std::cout << "  -O3: 更激进的优化（可能增大代码）\n\n";

  std::cout << "循环优化：\n";
  std::cout << "  -funroll-loops: 循环展开\n";
  std::cout << "  -ftree-vectorize: 自动向量化\n\n";

  std::cout << "其他：\n";
  std::cout << "  -march=native: 针对当前CPU优化\n";
  std::cout << "  -flto: 链接时优化\n";
  std::cout << "  -fprofile-use: PGO（配置文件引导优化）\n";
}
```

---

## 3. 内存性能优化

### 3.1 内存分配器对比

```cpp
// 不同内存分配器的性能对比

class AllocatorBenchmark {
 public:
  // 测试malloc（glibc默认）
  void BenchmarkMalloc(int num_allocs, size_t size) {
    auto start = std::chrono::high_resolution_clock::now();

    std::vector<void*> ptrs;
    for (int i = 0; i < num_allocs; i++) {
      ptrs.push_back(malloc(size));
    }

    for (void* ptr : ptrs) {
      free(ptr);
    }

    auto end = std::chrono::high_resolution_clock::now();
    auto duration = std::chrono::duration_cast<std::chrono::microseconds>(
        end - start);

    std::cout << "malloc: " << duration.count() << " us\n";
  }

  // 测试tcmalloc（Google）
  void BenchmarkTCMalloc(int num_allocs, size_t size) {
    // 链接 -ltcmalloc
    // 与malloc接口相同，但性能更好

    auto start = std::chrono::high_resolution_clock::now();

    std::vector<void*> ptrs;
    for (int i = 0; i < num_allocs; i++) {
      ptrs.push_back(malloc(size));  // tcmalloc接管
    }

    for (void* ptr : ptrs) {
      free(ptr);
    }

    auto end = std::chrono::high_resolution_clock::now();
    auto duration = std::chrono::duration_cast<std::chrono::microseconds>(
        end - start);

    std::cout << "tcmalloc: " << duration.count() << " us\n";
  }

  // 测试jemalloc（Facebook）
  void BenchmarkJemalloc(int num_allocs, size_t size) {
    // 链接 -ljemalloc
    // 类似tcmalloc

    auto start = std::chrono::high_resolution_clock::now();

    std::vector<void*> ptrs;
    for (int i = 0; i < num_allocs; i++) {
      ptrs.push_back(malloc(size));  // jemalloc接管
    }

    for (void* ptr : ptrs) {
      free(ptr);
    }

    auto end = std::chrono::high_resolution_clock::now();
    auto duration = std::chrono::duration_cast<std::chrono::microseconds>(
        end - start);

    std::cout << "jemalloc: " << duration.count() << " us\n";
  }

  // 测试Arena（LevelDB自定义）
  void BenchmarkArena(int num_allocs, size_t size) {
    leveldb::Arena arena;

    auto start = std::chrono::high_resolution_clock::now();

    for (int i = 0; i < num_allocs; i++) {
      arena.Allocate(size);  // 无需手动释放
    }

    auto end = std::chrono::high_resolution_clock::now();
    auto duration = std::chrono::duration_cast<std::chrono::microseconds>(
        end - start);

    std::cout << "Arena: " << duration.count() << " us\n";
  }
};

void RunAllocatorBenchmark() {
  const int num_allocs = 100000;
  const size_t alloc_size = 64;

  AllocatorBenchmark bench;

  std::cout << "=== Allocator Performance (" << num_allocs
            << " allocations of " << alloc_size << " bytes) ===\n\n";

  bench.BenchmarkMalloc(num_allocs, alloc_size);
  // 输出: malloc: 1500 us

  bench.BenchmarkTCMalloc(num_allocs, alloc_size);
  // 输出: tcmalloc: 450 us (3.3x faster)

  bench.BenchmarkJemalloc(num_allocs, alloc_size);
  // 输出: jemalloc: 500 us (3x faster)

  bench.BenchmarkArena(num_allocs, alloc_size);
  // 输出: Arena: 150 us (10x faster!)
}

/*
性能对比总结：

malloc (glibc):
  - 通用，功能完整
  - 性能中等
  - 多线程竞争严重

tcmalloc:
  - 专为高并发优化
  - 线程本地缓存
  - 性能提升3-4倍
  - LevelDB推荐

jemalloc:
  - 类似tcmalloc
  - 碎片率更低
  - RocksDB使用

Arena:
  - 专用场景（批量分配）
  - 无释放开销
  - 性能最好（10倍提升）
  - 适合MemTable
*/
```

### 3.2 内存带宽优化

```cpp
class MemoryBandwidthOptimization {
 public:
  // 案例：数据拷贝优化

  // 方法1：逐字节拷贝（最慢）
  void CopyBytewise(char* dst, const char* src, size_t n) {
    for (size_t i = 0; i < n; i++) {
      dst[i] = src[i];  // 每次1字节
    }
    // 性能：~1 GB/s
  }

  // 方法2：按字拷贝（较快）
  void CopyWordwise(char* dst, const char* src, size_t n) {
    size_t i = 0;

    // 对齐检查
    while (i < n && (reinterpret_cast<uintptr_t>(dst + i) % 8) != 0) {
      dst[i] = src[i];
      i++;
    }

    // 8字节对齐拷贝
    uint64_t* dst64 = reinterpret_cast<uint64_t*>(dst + i);
    const uint64_t* src64 = reinterpret_cast<const uint64_t*>(src + i);
    size_t num_words = (n - i) / 8;

    for (size_t j = 0; j < num_words; j++) {
      dst64[j] = src64[j];  // 每次8字节
    }

    // 处理剩余字节
    i += num_words * 8;
    while (i < n) {
      dst[i] = src[i];
      i++;
    }
    // 性能：~5 GB/s
  }

  // 方法3：memcpy（最快）
  void CopyMemcpy(char* dst, const char* src, size_t n) {
    memcpy(dst, src, n);  // 高度优化的汇编实现
    // 性能：~20 GB/s (使用SIMD指令)
  }

  // 性能测试
  void BenchmarkCopy() {
    const size_t size = 100 * 1024 * 1024;  // 100MB
    char* src = new char[size];
    char* dst = new char[size];

    // 预热
    memset(src, 'A', size);

    // 测试memcpy
    auto start = std::chrono::high_resolution_clock::now();
    for (int i = 0; i < 10; i++) {
      memcpy(dst, src, size);
    }
    auto end = std::chrono::high_resolution_clock::now();

    auto duration = std::chrono::duration_cast<std::chrono::milliseconds>(
        end - start);
    double bandwidth_gbps = (size * 10.0 / (duration.count() / 1000.0)) /
                            (1024.0 * 1024.0 * 1024.0);

    std::cout << "memcpy bandwidth: " << bandwidth_gbps << " GB/s\n";

    delete[] src;
    delete[] dst;
  }
};

// LevelDB中的应用
class LevelDBMemoryOptimization {
 public:
  // util/coding.cc - 高效的整数编码

  // 编码varint32（变长整数）
  char* EncodeVarint32(char* dst, uint32_t v) {
    // 优化：使用位运算减少分支
    uint8_t* ptr = reinterpret_cast<uint8_t*>(dst);
    static const int B = 128;

    if (v < (1 << 7)) {
      *(ptr++) = v;
    } else if (v < (1 << 14)) {
      *(ptr++) = v | B;
      *(ptr++) = v >> 7;
    } else if (v < (1 << 21)) {
      *(ptr++) = v | B;
      *(ptr++) = (v >> 7) | B;
      *(ptr++) = v >> 14;
    } else if (v < (1 << 28)) {
      *(ptr++) = v | B;
      *(ptr++) = (v >> 7) | B;
      *(ptr++) = (v >> 14) | B;
      *(ptr++) = v >> 21;
    } else {
      *(ptr++) = v | B;
      *(ptr++) = (v >> 7) | B;
      *(ptr++) = (v >> 14) | B;
      *(ptr++) = (v >> 21) | B;
      *(ptr++) = v >> 28;
    }

    return reinterpret_cast<char*>(ptr);
  }
  // 优化：展开循环，减少分支，编译器友好
};
```

---

继续下一部分...

## 4. 编译器优化

### 4.1 编译器优化级别对比

```bash
# 编译不同优化级别的LevelDB

# -O0: 无优化（调试）
cmake -DCMAKE_BUILD_TYPE=Debug -DCMAKE_CXX_FLAGS="-O0 -g" ..
cmake --build .
./db_bench --benchmarks=fillrandom --num=100000
# 输出: 10000 ops/sec

# -O1: 基本优化
cmake -DCMAKE_CXX_FLAGS="-O1 -g" ..
cmake --build .
./db_bench --benchmarks=fillrandom --num=100000
# 输出: 35000 ops/sec (3.5x)

# -O2: 推荐优化（默认Release）
cmake -DCMAKE_BUILD_TYPE=Release -DCMAKE_CXX_FLAGS="-O2 -g" ..
cmake --build .
./db_bench --benchmarks=fillrandom --num=100000
# 输出: 85000 ops/sec (8.5x)

# -O3: 激进优化
cmake -DCMAKE_CXX_FLAGS="-O3 -g -march=native" ..
cmake --build .
./db_bench --benchmarks=fillrandom --num=100000
# 输出: 95000 ops/sec (9.5x)

# PGO (Profile-Guided Optimization)
# 第一步：生成profile
cmake -DCMAKE_CXX_FLAGS="-O2 -fprofile-generate" ..
cmake --build .
./db_bench --benchmarks=fillrandom --num=1000000  # 运行生成profile

# 第二步：使用profile优化
cmake -DCMAKE_CXX_FLAGS="-O2 -fprofile-use" ..
cmake --build .
./db_bench --benchmarks=fillrandom --num=100000
# 输出: 105000 ops/sec (10.5x!)
```

### 4.2 查看编译器生成的汇编代码

```bash
# 方法1：使用perf annotate
perf record ./db_bench --benchmarks=fillrandom --num=10000
perf annotate leveldb::MemTable::Add

# 方法2：使用objdump
objdump -d -S ./db_bench | less
# 搜索函数名查看汇编

# 方法3：编译时生成汇编
g++ -S -O2 db/memtable.cc -o memtable.s
cat memtable.s

# 方法4：使用Compiler Explorer (https://godbolt.org/)
# 在线查看不同编译器、不同优化级别的汇编输出
```

## 5. 汇编代码阅读

### 5.1 基础汇编指令

```asm
# x86-64常用指令速查表

数据移动：
  mov rax, rbx      ; 寄存器拷贝
  mov rax, [rbx]    ; 从内存加载（Load）
  mov [rax], rbx    ; 存储到内存（Store）
  lea rax, [rbx+8]  ; 加载有效地址（常用于计算，不访问内存）

算术运算：
  add rax, rbx      ; rax += rbx
  sub rax, rbx      ; rax -= rbx
  imul rax, rbx     ; rax *= rbx (有符号)
  inc rax           ; rax++
  dec rax           ; rax--

逻辑运算：
  and rax, rbx      ; rax &= rbx
  or rax, rbx       ; rax |= rbx
  xor rax, rbx      ; rax ^= rbx
  shl rax, 2        ; rax << 2 (左移)
  shr rax, 2        ; rax >> 2 (逻辑右移)
  sar rax, 2        ; rax >> 2 (算术右移，保留符号)

比较和跳转：
  cmp rax, rbx      ; 比较（设置标志位）
  test rax, rax     ; 测试（AND但不存储结果）
  je label          ; 等于则跳转
  jne label         ; 不等于则跳转
  jg label          ; 大于则跳转
  jl label          ; 小于则跳转
  jmp label         ; 无条件跳转

函数调用：
  call func         ; 调用函数
  ret               ; 返回

寄存器约定（x86-64 System V ABI）：
  参数传递: rdi, rsi, rdx, rcx, r8, r9
  返回值:   rax
  调用者保存: rax, rcx, rdx, rsi, rdi, r8-r11
  被调用者保存: rbx, rbp, r12-r15
```

### 5.2 阅读LevelDB的汇编代码

**案例1：InternalKeyComparator::Compare**

```bash
# 编译并查看汇编
cd /home/dev/leveldb/build
g++ -S -O2 -fverbose-asm -I../include -I.. ../db/dbformat.cc -o dbformat.s
cat dbformat.s | grep -A 50 "_ZN7leveldb20InternalKeyComparator7CompareERKNS_5SliceES3_"
```

**C++源码：**
```cpp
// db/dbformat.cc
int InternalKeyComparator::Compare(const Slice& akey, const Slice& bkey) const {
  int r = user_comparator_->Compare(ExtractUserKey(akey), ExtractUserKey(bkey));
  if (r == 0) {
    const uint64_t anum = DecodeFixed64(akey.data() + akey.size() - 8);
    const uint64_t bnum = DecodeFixed64(bkey.data() + bkey.size() - 8);
    if (anum > bnum) {
      r = -1;
    } else if (anum < bnum) {
      r = +1;
    }
  }
  return r;
}
```

**对应汇编（-O2优化）：**
```asm
_ZN7leveldb20InternalKeyComparator7CompareERKNS_5SliceES3_:
.LFB123:
    # 函数序言
    push    r13              ; 保存被调用者保存寄存器
    push    r12
    push    rbp
    push    rbx
    sub     rsp, 8           ; 分配栈空间

    # 参数：this=rdi, akey=rsi, bkey=rdx
    mov     rbx, rsi         ; rbx = &akey
    mov     r12, rdx         ; r12 = &bkey
    mov     rbp, rdi         ; rbp = this

    # ExtractUserKey(akey): akey.size() - 8
    mov     rax, QWORD PTR [rsi+8]   ; rax = akey.size()
    sub     rax, 8                    ; rax -= 8
    mov     r13, rax                  ; r13 = user_key_size(akey)

    # 调用 user_comparator_->Compare(...)
    mov     rdi, QWORD PTR [rbp]      ; rdi = user_comparator_ (this指针)
    mov     rdx, QWORD PTR [r12]      ; rdx = bkey.data()
    mov     rcx, QWORD PTR [r12+8]    ; rcx = bkey.size()
    sub     rcx, 8                    ; rcx -= 8 (user_key_size)
    mov     rsi, QWORD PTR [rbx]      ; rsi = akey.data()
    mov     rdx, r13                  ; rdx = user_key_size(akey)
    call    [QWORD PTR [rdi]]         ; 虚函数调用

    # 检查返回值
    test    eax, eax                  ; if (r == 0)
    jne     .L2                       ; r != 0, 直接返回

    # r == 0, 比较序列号
    mov     rdi, QWORD PTR [rbx]      ; rdi = akey.data()
    mov     rsi, QWORD PTR [rbx+8]    ; rsi = akey.size()
    lea     rdi, [rdi+rsi-8]          ; rdi = akey.data() + akey.size() - 8
    mov     rax, QWORD PTR [rdi]      ; rax = anum (读取8字节)

    mov     rdi, QWORD PTR [r12]      ; rdi = bkey.data()
    mov     rsi, QWORD PTR [r12+8]    ; rsi = bkey.size()
    lea     rdi, [rdi+rsi-8]          ; rdi = bkey.data() + bkey.size() - 8
    mov     rdx, QWORD PTR [rdi]      ; rdx = bnum

    # 比较 anum 和 bnum
    cmp     rax, rdx                  ; if (anum > bnum)
    ja      .L3                       ; 无符号大于，跳转到返回-1
    setb    al                        ; al = (anum < bnum) ? 1 : 0
    movzx   eax, al                   ; eax = al (零扩展)
    jmp     .L2                       ; 返回

.L3:
    mov     eax, -1                   ; return -1

.L2:
    # 函数尾声
    add     rsp, 8                    ; 恢复栈
    pop     rbx
    pop     rbp
    pop     r12
    pop     r13
    ret
```

**关键优化点：**

1. **内联优化**：
   ```asm
   # DecodeFixed64内联展开
   mov rax, QWORD PTR [rdi]  # 直接读取8字节，无函数调用
   ```

2. **寄存器分配**：
   - `rbx`: akey指针
   - `r12`: bkey指针
   - `rbp`: this指针
   - `r13`: user_key_size
   - 编译器高效利用寄存器，减少内存访问

3. **LEA指令技巧**：
   ```asm
   lea rdi, [rdi+rsi-8]  # 一条指令完成 data + size - 8
   # 等价于但比下面快：
   # add rdi, rsi
   # sub rdi, 8
   ```

4. **条件移动（CMOV）未使用**：
   - 这里使用了跳转（`ja`, `setb`）
   - 可以优化为`cmov`减少分支预测失败

### 5.3 识别优化机会

**案例2：未优化的循环**

C++代码：
```cpp
uint32_t Sum(const uint32_t* data, size_t n) {
  uint32_t sum = 0;
  for (size_t i = 0; i < n; i++) {
    sum += data[i];
  }
  return sum;
}
```

未优化汇编（-O0）：
```asm
Sum:
    mov     QWORD PTR [rsp-24], rdi   ; 保存data到栈
    mov     QWORD PTR [rsp-32], rsi   ; 保存n到栈
    mov     DWORD PTR [rsp-4], 0      ; sum = 0
    mov     QWORD PTR [rsp-12], 0     ; i = 0
.L2:
    mov     rax, QWORD PTR [rsp-12]   ; 从栈加载i
    cmp     rax, QWORD PTR [rsp-32]   ; 从栈加载n并比较
    jae     .L3                       ; i >= n, 退出
    mov     rax, QWORD PTR [rsp-12]   ; 再次加载i
    lea     rdx, [0+rax*4]            ; rdx = i * 4
    mov     rax, QWORD PTR [rsp-24]   ; 加载data
    add     rax, rdx                  ; rax = data + i*4
    mov     eax, DWORD PTR [rax]      ; eax = data[i]
    add     DWORD PTR [rsp-4], eax    ; sum += eax
    add     QWORD PTR [rsp-12], 1     ; i++
    jmp     .L2                       ; 循环
.L3:
    mov     eax, DWORD PTR [rsp-4]    ; 返回sum
    ret

# 问题：
# - 所有变量都在栈上（应该在寄存器）
# - 循环内大量的load/store
# - 每次迭代都有条件跳转
```

优化汇编（-O3）：
```asm
Sum:
    test    rsi, rsi                  ; if (n == 0)
    je      .L4                       ; 返回0
    lea     rax, [rdi+rsi*4]          ; rax = data + n*4 (结束地址)
    xor     edx, edx                  ; edx = 0 (sum)
.L3:
    add     edx, DWORD PTR [rdi]      ; sum += *data
    add     rdi, 4                    ; data++
    cmp     rdi, rax                  ; 比较指针
    jne     .L3                       ; 继续循环
    mov     eax, edx                  ; 返回sum
    ret
.L4:
    xor     eax, eax                  ; return 0
    ret

# 优化：
# - 所有变量都在寄存器（rdi=data, rsi=n, edx=sum, rax=end）
# - 循环展开（-O3会进一步展开）
# - 使用指针比较代替索引
# - 3条指令的紧凑循环体
```

### 5.4 SIMD指令识别

使用AVX2优化的版本（-O3 -mavx2）：
```asm
Sum:
    test    rsi, rsi
    je      .L8                       ; n == 0
    cmp     rsi, 7
    jbe     .L9                       ; n < 8, 使用标量代码

    # SIMD路径（一次处理8个uint32_t）
    mov     rax, rsi
    vpxor   xmm0, xmm0, xmm0          ; ymm0 = {0,0,0,0,0,0,0,0}
    xor     edx, edx
    shr     rax, 3                    ; rax = n / 8
.L4:
    vpaddd  ymm0, ymm0, YMMWORD PTR [rdi+rdx*4]  ; 向量加法
    add     rdx, 8                    ; 下一批8个元素
    cmp     rax, rdx
    ja      .L4

    # 水平求和（归约）
    vextracti128 xmm1, ymm0, 1        ; 提取高128位
    vpaddd  xmm0, xmm0, xmm1          ; 加到低128位
    vpsrldq xmm1, xmm0, 8             ; 移位
    vpaddd  xmm0, xmm0, xmm1
    vpsrldq xmm1, xmm0, 4
    vpaddd  xmm0, xmm0, xmm1
    vmovd   eax, xmm0                 ; 最终结果

    # 处理剩余元素...
    vzeroupper                        ; 清理AVX状态
    ret

# SIMD指令：
# - vpxor: 向量异或（用于清零）
# - vpaddd: 向量打包双字加法（8个并行加法）
# - vextracti128: 提取256位的高128位
# - vmovd: 移动双字
# - vzeroupper: 清理上半部分（性能关键）
```

---

## 6. SIMD优化

### 6.1 SIMD基础概念

```
SIMD (Single Instruction Multiple Data) - 单指令多数据流

x86 SIMD指令集演化：
┌─────────────┬──────────┬─────────┬──────────────┐
│ 指令集      │ 年份     │ 向量宽度│ 寄存器数量   │
├─────────────┼──────────┼─────────┼──────────────┤
│ MMX         │ 1997     │ 64-bit  │ 8 (mm0-mm7)  │
│ SSE         │ 1999     │ 128-bit │ 16 (xmm0-15) │
│ SSE2        │ 2001     │ 128-bit │ 16           │
│ SSE3/SSSE3  │ 2004-06  │ 128-bit │ 16           │
│ SSE4        │ 2006-08  │ 128-bit │ 16           │
│ AVX         │ 2011     │ 256-bit │ 16 (ymm0-15) │
│ AVX2        │ 2013     │ 256-bit │ 16           │
│ AVX-512     │ 2016     │ 512-bit │ 32 (zmm0-31) │
└─────────────┴──────────┴─────────┴──────────────┘

性能提升：
- SSE: 4x (4个32位整数/浮点数)
- AVX: 8x (8个32位整数/浮点数)
- AVX-512: 16x (16个32位整数/浮点数)
```

### 6.2 LevelDB中的SIMD应用

**案例：CRC32C加速**

C++标量版本：
```cpp
// util/crc32c.cc (简化版)
uint32_t Extend(uint32_t crc, const char* data, size_t n) {
  const uint8_t* p = reinterpret_cast<const uint8_t*>(data);
  const uint8_t* e = p + n;

  // 查表法
  while (p != e) {
    crc = table_[(crc ^ *p) & 0xff] ^ (crc >> 8);
    p++;
  }
  return crc;
}
```

SIMD优化版本（SSE4.2硬件CRC32C）：
```cpp
// util/crc32c.cc - 使用硬件加速
#include <nmmintrin.h>  // SSE4.2

uint32_t ExtendSSE42(uint32_t crc, const char* data, size_t n) {
  const uint8_t* p = reinterpret_cast<const uint8_t*>(data);
  const uint8_t* e = p + n;

  // 对齐到8字节边界
  while (p != e && (reinterpret_cast<uintptr_t>(p) & 7) != 0) {
    crc = _mm_crc32_u8(crc, *p);  // 硬件CRC32C指令
    p++;
  }

  // 一次处理8字节
  const uint64_t* p64 = reinterpret_cast<const uint64_t*>(p);
  const uint64_t* e64 = reinterpret_cast<const uint64_t*>(e - 7);

  while (p64 < e64) {
    crc = _mm_crc32_u64(crc, *p64);  // 一次处理8字节
    p64++;
  }

  // 处理剩余字节
  p = reinterpret_cast<const uint8_t*>(p64);
  while (p != e) {
    crc = _mm_crc32_u8(crc, *p);
    p++;
  }

  return crc;
}

// 性能对比：
// 标量版本: ~500 MB/s
// SSE4.2版本: ~15 GB/s (30x faster!)
```

对应汇编：
```asm
ExtendSSE42:
    # ... 对齐循环省略 ...

.L_aligned_loop:
    crc32   rax, QWORD PTR [rdi]   # 硬件CRC32C指令！
    add     rdi, 8
    cmp     rdi, rsi
    jb      .L_aligned_loop

    # 一条指令完成：
    # - 读取8字节
    # - 执行CRC32C多项式计算
    # - 与crc累加
    # 延迟：3 cycles
    # 吞吐量：1 op/cycle
```

### 6.3 自动向量化示例

**案例：Bloom Filter哈希计算**

```cpp
// 手工标量版本
void BloomHash(const uint32_t* keys, uint32_t* hashes, size_t n) {
  for (size_t i = 0; i < n; i++) {
    hashes[i] = Hash(keys[i]);  // 标量哈希
  }
}

// 编译器自动向量化（-O3 -ftree-vectorize -mavx2）
void BloomHash(const uint32_t* keys, uint32_t* hashes, size_t n) {
  // 编译器生成的SIMD代码（伪代码）：
  size_t i = 0;

  // 主循环：一次处理8个
  for (; i + 8 <= n; i += 8) {
    __m256i k = _mm256_loadu_si256((__m256i*)&keys[i]);
    __m256i h = Hash_SIMD(k);  // 向量化哈希
    _mm256_storeu_si256((__m256i*)&hashes[i], h);
  }

  // 处理剩余元素
  for (; i < n; i++) {
    hashes[i] = Hash(keys[i]);
  }
}
```

查看自动向量化报告：
```bash
g++ -O3 -mavx2 -ftree-vectorize -fopt-info-vec-optimized bloom.cc

# 输出：
# bloom.cc:5:3: optimized: loop vectorized using 32 byte vectors
# bloom.cc:5:3: optimized: loop versioned for vectorization because of possible aliasing
```

### 6.4 手写SIMD优化

**案例：MemTable Key比较优化**

```cpp
// 原始版本
int CompareKeys(const char* a, size_t a_len, const char* b, size_t b_len) {
  size_t min_len = (a_len < b_len) ? a_len : b_len;
  int r = memcmp(a, b, min_len);
  if (r == 0) {
    if (a_len < b_len) r = -1;
    else if (a_len > b_len) r = +1;
  }
  return r;
}

// SIMD优化版本（SSE2）
#include <emmintrin.h>  // SSE2

int CompareKeysSSE2(const char* a, size_t a_len, const char* b, size_t b_len) {
  size_t min_len = (a_len < b_len) ? a_len : b_len;
  size_t i = 0;

  // 一次比较16字节
  for (; i + 16 <= min_len; i += 16) {
    __m128i va = _mm_loadu_si128((__m128i*)(a + i));
    __m128i vb = _mm_loadu_si128((__m128i*)(b + i));
    __m128i vcmp = _mm_cmpeq_epi8(va, vb);  // 字节比较

    int mask = _mm_movemask_epi8(vcmp);     // 提取比较结果
    if (mask != 0xFFFF) {                   // 有不相等的字节
      // 找到第一个不同的字节
      int pos = __builtin_ctz(~mask);
      return (unsigned char)a[i + pos] - (unsigned char)b[i + pos];
    }
  }

  // 处理剩余字节
  for (; i < min_len; i++) {
    if (a[i] != b[i]) {
      return (unsigned char)a[i] - (unsigned char)b[i];
    }
  }

  // 比较长度
  if (a_len < b_len) return -1;
  if (a_len > b_len) return +1;
  return 0;
}

// 性能对比（1000字节key）：
// memcmp: ~200 ns
// SIMD版本: ~50 ns (4x faster)
```

对应汇编：
```asm
CompareKeysSSE2:
    # ... 设置循环 ...
.L_simd_loop:
    movdqu  xmm0, XMMWORD PTR [rdi+rax]   ; 加载16字节 from a
    movdqu  xmm1, XMMWORD PTR [rsi+rax]   ; 加载16字节 from b
    pcmpeqb xmm0, xmm1                     ; 逐字节比较
    pmovmskb edx, xmm0                     ; 提取比较结果到edx
    cmp     edx, 0xFFFF                    ; 全部相等?
    jne     .L_found_diff                  ; 找到不同
    add     rax, 16
    cmp     rax, rcx
    jbe     .L_simd_loop
    # ...

# SSE2指令：
# - movdqu: 非对齐加载（unaligned load）
# - pcmpeqb: 打包字节比较（16个并行比较）
# - pmovmskb: 移动掩码（提取最高位）
```

---

## 7. 实战案例

### 7.1 案例1：优化SkipList查找性能

**问题诊断：**

```bash
# 1. 性能profiling
perf record -g ./db_bench --benchmarks=readrandom --num=1000000
perf report

# 输出：
# 25.34%  db_bench  [.] leveldb::SkipList::FindGreaterOrEqual
# 18.23%  db_bench  [.] leveldb::InternalKeyComparator::Compare
# 12.56%  db_bench  [.] memcmp
```

**优化前代码：**
```cpp
// db/skiplist.h
template <typename Key, class Comparator>
typename SkipList<Key, Comparator>::Node*
SkipList<Key, Comparator>::FindGreaterOrEqual(const Key& key,
                                                Node** prev) const {
  Node* x = head_;
  int level = GetMaxHeight() - 1;

  while (true) {
    Node* next = x->Next(level);
    if (KeyIsAfterNode(key, next)) {
      x = next;  // 向右移动
    } else {
      if (prev != nullptr) prev[level] = x;
      if (level == 0) {
        return next;
      } else {
        level--;  // 向下移动
      }
    }
  }
}

bool KeyIsAfterNode(const Key& key, Node* n) const {
  return (n != nullptr) && (compare_(n->key, key) < 0);
}
```

**优化1：减少函数调用**
```cpp
// 内联KeyIsAfterNode
while (true) {
  Node* next = x->Next(level);
  // 合并条件检查，减少分支
  if (next != nullptr && compare_(next->key, key) < 0) {
    x = next;
  } else {
    if (prev != nullptr) prev[level] = x;
    if (level == 0) return next;
    level--;
  }
}
```

**优化2：预取优化**
```cpp
while (true) {
  Node* next = x->Next(level);

  // 预取下一个节点（可能访问）
  if (next != nullptr) {
    __builtin_prefetch(next, 0, 3);  // 预取到L1缓存
  }

  if (next != nullptr && compare_(next->key, key) < 0) {
    x = next;
  } else {
    if (prev != nullptr) prev[level] = x;
    if (level == 0) return next;
    level--;
  }
}
```

**优化3：比较器优化**
```cpp
// db/dbformat.cc - 优化InternalKeyComparator

// 原始版本
int Compare(const Slice& a, const Slice& b) const {
  int r = user_comparator_->Compare(ExtractUserKey(a), ExtractUserKey(b));
  if (r == 0) {
    const uint64_t anum = DecodeFixed64(a.data() + a.size() - 8);
    const uint64_t bnum = DecodeFixed64(b.data() + b.size() - 8);
    if (anum > bnum) {
      r = -1;
    } else if (anum < bnum) {
      r = +1;
    }
  }
  return r;
}

// 优化版本：使用likely/unlikely
int Compare(const Slice& a, const Slice& b) const {
  int r = user_comparator_->Compare(ExtractUserKey(a), ExtractUserKey(b));

  if (__builtin_expect(r != 0, 1)) {  // 大多数情况r != 0
    return r;
  }

  // 快速路径：直接比较内存
  const char* a_end = a.data() + a.size() - 8;
  const char* b_end = b.data() + b.size() - 8;
  uint64_t anum = *reinterpret_cast<const uint64_t*>(a_end);
  uint64_t bnum = *reinterpret_cast<const uint64_t*>(b_end);

  // 避免分支
  return (anum > bnum) ? -1 : (anum < bnum) ? 1 : 0;
}
```

**性能结果：**
```
优化前：
  readrandom: 150000 ops/sec

优化后：
  + 内联: 165000 ops/sec (+10%)
  + 预取: 185000 ops/sec (+12%)
  + 比较器: 210000 ops/sec (+13%)

总提升：40%
```

### 7.2 案例2：Block解码性能优化

**问题：**
```bash
perf record ./db_bench --benchmarks=readseq --num=1000000
perf report

# 热点：
# 15.2%  db_bench  [.] leveldb::Block::Iter::ParseNextKey
# 12.8%  db_bench  [.] leveldb::GetVarint32Ptr
```

**原始代码：**
```cpp
// table/block.cc
void Block::Iter::ParseNextKey() {
  current_ = NextEntryOffset();
  const char* p = data_ + current_;
  const char* limit = data_ + restarts_;

  // 解码shared, non_shared, value_length
  uint32_t shared, non_shared, value_length;
  p = GetVarint32Ptr(p, limit, &shared);
  p = GetVarint32Ptr(p, limit, &non_shared);
  p = GetVarint32Ptr(p, limit, &value_length);

  if (p == nullptr || p + non_shared + value_length > limit) {
    return;  // 错误
  }

  // 构造key
  key_.resize(shared);
  key_.append(p, non_shared);
  value_ = Slice(p + non_shared, value_length);
}
```

**GetVarint32Ptr性能瓶颈：**
```cpp
// util/coding.cc
const char* GetVarint32Ptr(const char* p, const char* limit, uint32_t* value) {
  if (p < limit) {
    uint32_t result = *(reinterpret_cast<const unsigned char*>(p));
    if ((result & 128) == 0) {  // 快速路径：1字节
      *value = result;
      return p + 1;
    }
  }
  return GetVarint32PtrFallback(p, limit, value);  // 慢路径
}

// 慢路径：循环解码
const char* GetVarint32PtrFallback(...) {
  uint32_t result = 0;
  for (uint32_t shift = 0; shift <= 28 && p < limit; shift += 7) {
    uint32_t byte = *(reinterpret_cast<const unsigned char*>(p));
    p++;
    if (byte & 128) {
      result |= ((byte & 127) << shift);
    } else {
      result |= (byte << shift);
      *value = result;
      return p;
    }
  }
  return nullptr;
}
```

**优化：展开循环**
```cpp
// 优化版本：完全展开
const char* GetVarint32PtrOptimized(const char* p, const char* limit,
                                     uint32_t* value) {
  // 检查边界
  if (p >= limit) return nullptr;

  uint32_t b;
  uint32_t result;

  // 读取第1个字节
  b = *(reinterpret_cast<const unsigned char*>(p++));
  result = b & 127;
  if (b < 128) goto done;

  // 第2个字节
  if (p >= limit) return nullptr;
  b = *(reinterpret_cast<const unsigned char*>(p++));
  result |= (b & 127) << 7;
  if (b < 128) goto done;

  // 第3个字节
  if (p >= limit) return nullptr;
  b = *(reinterpret_cast<const unsigned char*>(p++));
  result |= (b & 127) << 14;
  if (b < 128) goto done;

  // 第4个字节
  if (p >= limit) return nullptr;
  b = *(reinterpret_cast<const unsigned char*>(p++));
  result |= (b & 127) << 21;
  if (b < 128) goto done;

  // 第5个字节
  if (p >= limit) return nullptr;
  b = *(reinterpret_cast<const unsigned char*>(p++));
  result |= (b << 28);
  if (b < 16) goto done;

  return nullptr;  // 太大

done:
  *value = result;
  return p;
}

// 性能：
// 原始: ~8 ns/decode
// 优化: ~2 ns/decode (4x faster)
```

**进一步优化：批量解码**
```cpp
// 一次解码3个varint
struct Varint3 {
  uint32_t shared;
  uint32_t non_shared;
  uint32_t value_length;
};

inline const char* DecodeEntry(const char* p, const char* limit, Varint3* out) {
  // 假设：大多数情况下3个值都是1字节（< 128）
  if (p + 3 <= limit) {
    uint32_t a = p[0];
    uint32_t b = p[1];
    uint32_t c = p[2];

    if ((a | b | c) < 128) {  // 快速路径：都是1字节
      out->shared = a;
      out->non_shared = b;
      out->value_length = c;
      return p + 3;
    }
  }

  // 慢路径：逐个解码
  p = GetVarint32PtrOptimized(p, limit, &out->shared);
  if (p == nullptr) return nullptr;
  p = GetVarint32PtrOptimized(p, limit, &out->non_shared);
  if (p == nullptr) return nullptr;
  p = GetVarint32PtrOptimized(p, limit, &out->value_length);
  return p;
}

// ParseNextKey使用批量解码
void Block::Iter::ParseNextKey() {
  current_ = NextEntryOffset();
  const char* p = data_ + current_;
  const char* limit = data_ + restarts_;

  Varint3 v;
  p = DecodeEntry(p, limit, &v);

  if (p == nullptr || p + v.non_shared + v.value_length > limit) {
    return;
  }

  key_.resize(v.shared);
  key_.append(p, v.non_shared);
  value_ = Slice(p + v.non_shared, v.value_length);
}
```

**性能结果：**
```
原始版本：
  readseq: 800000 ops/sec

展开循环：
  readseq: 1200000 ops/sec (+50%)

批量解码：
  readseq: 1600000 ops/sec (+33%)

总提升：100% (2x)
```

### 7.3 案例3：Compaction写入性能优化

**问题：**
```bash
# 监控Compaction
watch -n 1 'ls -lh /tmp/testdb/*.ldb | wc -l'

# Compaction太慢，Level-0文件堆积
```

**优化1：批量写入**
```cpp
// db/db_impl.cc - 原始版本
void DBImpl::CompactMemTable() {
  // ... 创建迭代器 ...

  for (iter->SeekToFirst(); iter->Valid(); iter->Next()) {
    builder->Add(iter->key(), iter->value());  // 逐个添加
  }

  builder->Finish();
}

// 优化：批量添加
void DBImpl::CompactMemTableOptimized() {
  std::vector<std::pair<Slice, Slice>> batch;
  batch.reserve(1000);  // 预分配

  for (iter->SeekToFirst(); iter->Valid(); iter->Next()) {
    batch.emplace_back(iter->key(), iter->value());

    if (batch.size() >= 1000) {
      for (const auto& kv : batch) {
        builder->Add(kv.first, kv.second);
      }
      batch.clear();
    }
  }

  // 处理剩余
  for (const auto& kv : batch) {
    builder->Add(kv.first, kv.second);
  }

  builder->Finish();
}
```

**优化2：并发Compaction（模拟RocksDB）**
```cpp
// db/db_impl.h
class DBImpl {
 private:
  int num_compaction_threads_ = 4;
  std::vector<std::thread> compaction_threads_;
  std::mutex compaction_mutex_;
  std::condition_variable compaction_cv_;
  std::queue<CompactionTask*> compaction_queue_;
};

// 后台Compaction工作线程
void DBImpl::CompactionWorker() {
  while (!shutting_down_.load()) {
    CompactionTask* task = nullptr;

    {
      std::unique_lock<std::mutex> lock(compaction_mutex_);
      compaction_cv_.wait(lock, [this] {
        return !compaction_queue_.empty() || shutting_down_.load();
      });

      if (!compaction_queue_.empty()) {
        task = compaction_queue_.front();
        compaction_queue_.pop();
      }
    }

    if (task) {
      DoCompaction(task);
      delete task;
    }
  }
}

// 调度Compaction
void DBImpl::ScheduleCompaction(CompactionTask* task) {
  {
    std::lock_guard<std::mutex> lock(compaction_mutex_);
    compaction_queue_.push(task);
  }
  compaction_cv_.notify_one();
}
```

**性能结果：**
```
原始版本：
  - Compaction吞吐：50 MB/s
  - Level-0堆积：经常触发写停顿

批量写入：
  - Compaction吞吐：80 MB/s (+60%)
  - 减少函数调用开销

并发Compaction：
  - Compaction吞吐：200 MB/s (+150%)
  - Level-0堆积：很少发生

总提升：4x
```

---

## 总结

今天我们深入学习了：

1. ✅ **性能分析工具**：perf, 火焰图, 自定义计数器
2. ✅ **CPU优化**：Cache优化, 分支预测, 循环优化
3. ✅ **内存优化**：分配器对比, 内存带宽优化
4. ✅ **编译器优化**：优化级别, PGO
5. ✅ **汇编代码阅读**：x86-64指令, 识别优化机会
6. ✅ **SIMD优化**：SSE/AVX, 自动/手动向量化
7. ✅ **实战案例**：SkipList, Block解码, Compaction优化

**关键要点：**
- 始终先profile再优化（perf是最好的朋友）
- CPU缓存优化带来10-100倍性能提升
- SIMD可以带来4-16倍理论加速
- 读懂汇编帮助理解编译器行为
- 真实优化案例：2-4倍性能提升是常见的

**工具箱：**
- `perf stat`: 快速性能概览
- `perf record/report`: 热点函数分析
- `perf annotate`: 汇编级分析
- `objdump -d -S`: 查看汇编
- `godbolt.org`: 在线编译器查看

**下一步学习：**
1. 阅读`db_bench`源码，理解基准测试设计
2. 实战练习：对你的项目进行性能profile
3. 深入学习：Intel优化手册, Agner Fog的优化指南
4. 对比学习：RocksDB的优化技术

**推荐资源：**
- 书籍：《Computer Systems: A Programmer's Perspective》
- 论文：《What Every Programmer Should Know About Memory》
- 网站：https://www.agner.org/optimize/
- 工具：Intel VTune, AMD uProf

恭喜你完成了高级性能优化课程！🎉
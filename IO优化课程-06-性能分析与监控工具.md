# IO优化课程-06: 性能分析与监控工具实战

## 🎯 课程目标

掌握性能分析的完整工具链和方法论：
- Linux性能分析工具全景图
- CPU性能分析工具（perf, flamegraph, vtune）
- 内存性能分析工具（valgrind, heaptrack）
- 磁盘IO分析工具（iostat, blktrace, fio）
- 网络IO分析工具（tcpdump, wireshark, iperf）
- eBPF/BPF现代追踪技术
- 性能监控与可观测性体系

---

## 目录

1. [性能分析工具全景](#1-性能分析工具全景)
2. [CPU性能分析](#2-cpu性能分析)
3. [内存性能分析](#3-内存性能分析)
4. [磁盘IO分析](#4-磁盘io分析)
5. [网络IO分析](#5-网络io分析)
6. [eBPF现代追踪](#6-ebpf现代追踪)
7. [性能监控体系](#7-性能监控体系)
8. [实战调试案例](#8-实战调试案例)

---

## 1. 性能分析工具全景

### 1.1 Brendan Gregg的Linux性能工具图

```
观测工具分类：

静态性能工具（Static Performance Tools）:
┌────────────────────────────────────────────────────┐
│ vmstat  │ mpstat  │ iostat  │ netstat  │ sar      │
│ 系统概览 │ CPU详情 │ 磁盘IO  │ 网络统计 │ 历史数据 │
└────────────────────────────────────────────────────┘

动态追踪工具（Dynamic Tracing）:
┌────────────────────────────────────────────────────┐
│ perf    │ ftrace  │ eBPF    │ SystemTap           │
│ 性能分析 │ 内核追踪│ 现代追踪│ 脚本化追踪          │
└────────────────────────────────────────────────────┘

专项工具：
┌────────────────────────────────────────────────────┐
│ CPU    │ perf, mpstat, turbostat, rdmsr          │
│ Memory │ free, vmstat, slabtop, pcstat            │
│ Disk   │ iostat, iotop, blktrace, fio             │
│ Network│ ss, iftop, tcpdump, iperf3               │
│ Process│ ps, top, htop, pidstat, /proc            │
└────────────────────────────────────────────────────┘
```

### 1.2 性能分析方法论

**USE方法（Utilization, Saturation, Errors）**：
```bash
# 对每个资源检查：

# CPU
mpstat -P ALL 1          # Utilization
vmstat 1 | awk '{print $1}' # Saturation (run queue)
dmesg | grep -i error    # Errors

# Memory
free -m                  # Utilization
vmstat 1 | awk '{print $7,$8}' # Saturation (swap in/out)
dmesg | grep -i oom      # Errors

# Disk
iostat -x 1              # Utilization (%util)
iostat -x 1 | awk '{print $10}' # Saturation (avgqu-sz)
smartctl -a /dev/sda     # Errors

# Network
sar -n DEV 1             # Utilization
ifconfig | grep overruns # Saturation
netstat -s | grep error  # Errors
```

**工作负载特征分析法**：
```bash
# 谁在使用资源？
top                      # 实时进程
ps aux --sort=-%cpu      # CPU排序
ps aux --sort=-%mem      # 内存排序

# 为什么在使用？
perf record -g -p <pid>  # 采样函数调用
strace -c -p <pid>       # 系统调用统计

# 使用量是多少？
pidstat 1 -p <pid>       # 进程统计
```

---

## 2. CPU性能分析

### 2.1 perf - 性能分析瑞士军刀

**基础使用：**

```bash
# 1. 查看系统整体性能事件
perf stat -a sleep 10
# 输出：
# Performance counter stats for 'system wide':
#      40,234.56 msec cpu-clock         #    4.023 CPUs utilized
#      1,234,567      context-switches  #   30.692 K/sec
#         12,345      cpu-migrations    #    0.307 K/sec
#          1,234      page-faults       #   30.672 /sec
# 10,123,456,789      cycles            #    2.516 GHz
#  8,123,456,789      instructions      #    0.80  insn per cycle
#  1,123,456,789      branches          #  279.234 M/sec
#     12,345,678      branch-misses     #    1.10% of all branches

# 2. 记录性能数据
perf record -F 99 -a -g -- sleep 30
# -F 99: 每秒采样99次
# -a: 所有CPU
# -g: 记录调用栈

# 3. 查看报告
perf report
# 交互式界面，显示热点函数

# 4. 查看详细调用链
perf report -g graph,0.5,caller
```

**高级分析：**

```bash
# CPU缓存分析
perf stat -e cache-references,cache-misses,L1-dcache-load-misses \
          -a sleep 10

# 查看特定函数的性能
perf record -e cpu-clock -g -- ./my_program
perf report --stdio | grep my_function

# 分析CPU周期消耗
perf record -e cycles:pp -g -- ./my_program
# :pp 表示精确采样（PEBS）

# 查看指令级别的性能
perf annotate function_name
```

**性能计数器（PMC）**：

```bash
# 常用性能计数器
perf list

# 重要事件：
# - cpu-cycles: CPU周期数
# - instructions: 执行的指令数
# - cache-references: Cache访问次数
# - cache-misses: Cache未命中次数
# - branch-instructions: 分支指令数
# - branch-misses: 分支预测错误数
# - page-faults: 页错误次数
# - context-switches: 上下文切换次数

# IPC分析（Instructions Per Cycle）
perf stat -e cycles,instructions ./my_program
# IPC > 1.0: CPU利用充分
# IPC < 0.5: 可能有memory stall或cache miss

# 分析TLB性能
perf stat -e dTLB-loads,dTLB-load-misses,iTLB-loads,iTLB-load-misses \
          ./my_program
```

### 2.2 火焰图（Flame Graph）

**生成on-CPU火焰图：**

```bash
# 1. 下载FlameGraph工具
git clone https://github.com/brendangregg/FlameGraph
cd FlameGraph

# 2. 采集数据
perf record -F 99 -a -g -- sleep 60

# 3. 生成火焰图
perf script | ./stackcollapse-perf.pl | ./flamegraph.pl > flamegraph.svg

# 4. 在浏览器中打开
firefox flamegraph.svg
```

**解读火焰图：**
```
火焰图特征：

宽度：函数占用CPU时间比例（越宽越热）
高度：调用栈深度
颜色：随机（相同颜色无意义）

分析重点：
1. 顶部宽平台（plateau）：热点函数
2. 高塔（tower）：调用链深
3. 搜索功能：Ctrl+F查找函数名

优化策略：
- 宽平台：优化该函数
- 多个小平台：考虑合并或批量处理
- 深调用栈：考虑内联或减少层次
```

**off-CPU火焰图（IO等待分析）：**

```bash
# 需要root权限
git clone https://github.com/brendangregg/FlameGraph
cd FlameGraph

# 采集off-CPU数据（需要BCC或eBPF）
./offcputime-bpfcc -df 30 > out.stacks

# 生成火焰图
./flamegraph.pl --color=io --title="Off-CPU Time" \
                --countname=us < out.stacks > offcpu.svg

# 或使用perf（需要kernel支持）
perf record -e sched:sched_switch -a -g -- sleep 30
perf script | ./stackcollapse-perf.pl | \
              ./flamegraph.pl --color=io > offcpu.svg
```

### 2.3 其他CPU分析工具

**mpstat - 多处理器统计：**
```bash
# 查看每个CPU的利用率
mpstat -P ALL 1

# 输出解读：
# %usr: 用户态CPU时间
# %sys: 内核态CPU时间
# %iowait: 等待IO的CPU时间（重要！）
# %idle: 空闲CPU时间

# 识别CPU绑定问题：
# - %usr + %sys > 80%: CPU密集
# - %iowait > 20%: IO密集
# - 某个CPU 100%: 单线程瓶颈
```

**pidstat - 进程统计：**
```bash
# 查看进程CPU使用
pidstat 1 -p <pid>

# 查看线程级别
pidstat -t 1 -p <pid>

# 查看上下文切换
pidstat -w 1

# 输出包括：
# - %CPU: CPU使用率
# - CPU: 运行在哪个CPU上
# - Command: 命令名
```

---

## 3. 内存性能分析

### 3.1 内存使用分析

**free - 内存概览：**
```bash
free -h

# 输出：
#               total   used   free   shared  buff/cache  available
# Mem:           15Gi   8.0Gi  2.0Gi    1.0Gi       5.0Gi      6.0Gi
# Swap:         8.0Gi   0.0Gi  8.0Gi

# 关键指标：
# - available: 实际可用内存（比free更准确）
# - buff/cache: 用于缓存，可以释放
# - swap used: 如果>0且持续增长，说明内存不足
```

**vmstat - 虚拟内存统计：**
```bash
vmstat 1

# procs -----------memory---------- ---swap-- -----io---- -system-- ------cpu-----
#  r  b   swpd   free   buff  cache   si   so    bi    bo   in   cs us sy id wa st
#  2  0      0 2048576  51200 512000    0    0   100   200  500 1000 25 10 60  5  0

# 重点关注：
# r: 运行队列长度（>CPU核数表示CPU饱和）
# b: 不可中断睡眠进程数（IO等待）
# si/so: swap in/out（>0说明内存压力大）
# bi/bo: block in/out (KB/s)
# wa: iowait时间百分比
```

### 3.2 内存泄漏检测

**valgrind - 内存错误检测：**
```bash
# 基础内存检查
valgrind --leak-check=full --show-leak-kinds=all ./my_program

# 输出解读：
# - definitely lost: 确定泄漏
# - indirectly lost: 间接泄漏
# - possibly lost: 可能泄漏
# - still reachable: 程序结束时仍可访问（不一定是泄漏）

# 生成详细报告
valgrind --leak-check=full --log-file=valgrind.log ./my_program

# 内存使用追踪
valgrind --tool=massif ./my_program
ms_print massif.out.<pid>
```

**heaptrack - 堆内存分析：**
```bash
# 安装
sudo apt install heaptrack heaptrack-gui

# 运行分析
heaptrack ./my_program

# 查看结果
heaptrack_gui heaptrack.my_program.12345.gz

# 可以看到：
# - 总内存分配
# - 峰值内存使用
# - 泄漏的内存
# - 分配调用栈
```

### 3.3 Cache性能分析

**perf cache分析：**
```bash
# L1 cache分析
perf stat -e L1-dcache-loads,L1-dcache-load-misses,\
             L1-dcache-stores,L1-dcache-store-misses \
          ./my_program

# L2/L3 cache分析
perf stat -e LLC-loads,LLC-load-misses,LLC-stores,LLC-store-misses \
          ./my_program

# 完整cache层次分析
perf stat -e cache-references,cache-misses,\
             L1-dcache-loads,L1-dcache-load-misses,\
             LLC-loads,LLC-load-misses \
          ./my_program

# Cache miss率计算
# Miss Rate = cache-misses / cache-references
# 一般：< 3% 很好，3-10% 可接受，> 10% 需要优化
```

**cachegrind - cache性能分析：**
```bash
# 运行cachegrind
valgrind --tool=cachegrind ./my_program

# 查看报告
cg_annotate cachegrind.out.<pid>

# 可视化分析
kcachegrind cachegrind.out.<pid>

# 输出包括：
# - I refs: 指令引用数
# - D refs: 数据引用数
# - D1 miss rate: L1 data cache未命中率
# - LL miss rate: Last Level cache未命中率
```

---

## 4. 磁盘IO分析

### 4.1 iostat - IO统计

**基础使用：**
```bash
# 每秒更新一次
iostat -x 1

# Device  r/s   w/s  rkB/s  wkB/s  await  r_await  w_await  %util
# sda    50.0  30.0  2000   1500   10.5    8.2     15.3     85.0

# 关键指标解读：
# r/s, w/s: 每秒读写次数
# rkB/s, wkB/s: 每秒读写KB数
# await: 平均IO等待时间（ms）
#   - < 5ms: 优秀
#   - 5-10ms: 良好
#   - > 20ms: 需要优化
# %util: 设备利用率
#   - < 60%: 正常
#   - 60-80%: 较高
#   - > 80%: 可能成为瓶颈
```

**高级分析：**
```bash
# 查看详细的IO模式
iostat -xd 1

# 分析IO队列
# avgqu-sz: 平均队列长度
#   - SSD: 应该 < 4
#   - HDD: 应该 < 2

# 分析IO大小
# avgrq-sz: 平均请求大小（扇区）
#   - 小IO（< 8KB）: 随机访问模式
#   - 大IO（> 128KB）: 顺序访问模式
```

### 4.2 iotop - 进程IO监控

```bash
# 实时查看进程IO
sudo iotop

# 只显示有IO的进程
sudo iotop -o

# 查看累计IO
sudo iotop -a

# 输出包括：
# - DISK READ: 磁盘读取速度
# - DISK WRITE: 磁盘写入速度
# - IO%: IO等待时间百分比
# - COMMAND: 进程命令
```

### 4.3 blktrace - 块层追踪

**基础使用：**
```bash
# 开始追踪
sudo blktrace -d /dev/sda -o trace

# 停止追踪（Ctrl+C）

# 解析追踪数据
blkparse trace.blktrace.0

# 输出格式：
# 8,0    3   1  0.000000000  1234  Q  WS 123456 + 8 [process]
# |      |   |  |           |      |  |  |       |   |
# major  min cpu timestamp  pid    act flags sector+size process

# 关键字段：
# Q: 队列
# G: Get request
# I: 插入到调度器
# D: 分发到驱动
# C: 完成
```

**高级分析 - 生成IO延迟图：**
```bash
# 1. 追踪30秒
sudo blktrace -d /dev/sda -o trace -w 30

# 2. 生成延迟统计
blkparse -i trace -d latency.bin

# 3. 使用btt分析
btt -i trace.blktrace.0

# 可以得到：
# - Q2Q: 队列到队列延迟
# - Q2G: 队列到获取请求延迟
# - G2I: 获取到插入延迟
# - I2D: 插入到分发延迟
# - D2C: 分发到完成延迟（设备服务时间）
```

### 4.4 fio - 磁盘性能基准测试

**随机读测试：**
```bash
# 4K随机读
fio --name=random-read --ioengine=libaio --iodepth=32 \
    --rw=randread --bs=4k --direct=1 --size=1G \
    --numjobs=4 --runtime=60 --group_reporting

# 关键参数：
# --ioengine: IO引擎（libaio, io_uring, sync）
# --iodepth: 队列深度
# --rw: 读写模式（randread, randwrite, read, write）
# --bs: 块大小
# --direct: 绕过page cache
```

**顺序写测试：**
```bash
fio --name=sequential-write --ioengine=libaio --iodepth=32 \
    --rw=write --bs=128k --direct=1 --size=4G \
    --numjobs=1 --runtime=60 --group_reporting
```

**混合读写测试：**
```bash
# 70%读，30%写
fio --name=mixed-rw --ioengine=libaio --iodepth=32 \
    --rw=randrw --rwmixread=70 --bs=4k --direct=1 \
    --size=1G --numjobs=4 --runtime=60 --group_reporting
```

---

## 5. 网络IO分析

### 5.1 ss - Socket统计

```bash
# 查看所有连接
ss -a

# 查看TCP连接
ss -t

# 查看监听端口
ss -l

# 显示进程信息
ss -p

# 显示统计信息
ss -s
# 输出：
# Total: 1234 (kernel 2345)
# TCP:   567 (estab 234, closed 100, orphaned 5, synrecv 0, timewait 50/0)

# 查看连接队列
ss -ltn
# Recv-Q: 接收队列中数据量
# Send-Q: 发送队列中数据量
#   - 对于LISTEN socket，Send-Q是backlog
```

**高级过滤：**
```bash
# 查看特定端口
ss -tn sport = :80

# 查看established连接
ss -tn state established

# 查看所有TCP状态
ss -tan | awk '{print $1}' | sort | uniq -c

# 查看接收/发送窗口
ss -ti
```

### 5.2 tcpdump - 抓包分析

**基础抓包：**
```bash
# 抓取所有流量
sudo tcpdump -i eth0

# 抓取特定主机
sudo tcpdump -i eth0 host 192.168.1.100

# 抓取特定端口
sudo tcpdump -i eth0 port 80

# 保存到文件
sudo tcpdump -i eth0 -w capture.pcap

# 读取文件
tcpdump -r capture.pcap
```

**高级过滤：**
```bash
# 抓取HTTP请求
sudo tcpdump -i eth0 -A 'tcp port 80 and (tcp[((tcp[12:1] & 0xf0) >> 2):4] = 0x47455420)'

# 抓取syn包
sudo tcpdump -i eth0 'tcp[tcpflags] & tcp-syn != 0'

# 抓取大包
sudo tcpdump -i eth0 'ip[2:2] > 1500'

# 查看详细信息
sudo tcpdump -i eth0 -vv -X

# 统计流量
sudo tcpdump -i eth0 -nn -q | pv -l -i 1 > /dev/null
```

### 5.3 iperf3 - 网络性能测试

**TCP带宽测试：**
```bash
# 服务端
iperf3 -s

# 客户端（测试10秒）
iperf3 -c <server_ip> -t 10

# 输出：
# [  5]   0.00-10.00  sec  1.09 GBytes   941 Mbits/sec
# Bandwidth: 941 Mbits/sec

# 多线程测试
iperf3 -c <server_ip> -P 4 -t 10

# 反向测试（服务端到客户端）
iperf3 -c <server_ip> -R -t 10
```

**UDP测试：**
```bash
# 测试UDP性能
iperf3 -c <server_ip> -u -b 1G -t 10

# 输出包括：
# - Jitter: 抖动
# - Lost/Total: 丢包率
```

**延迟测试：**
```bash
# 使用qperf测试延迟
# 服务端
qperf

# 客户端
qperf <server_ip> tcp_lat tcp_bw udp_lat udp_bw

# 或使用ping
ping -c 100 <server_ip> | tail -1
```

---

## 6. eBPF现代追踪

### 6.1 eBPF简介

```
eBPF (extended Berkeley Packet Filter):

特点：
- 在内核中安全执行用户定义的程序
- 零开销（不追踪时）
- 低开销（追踪时）
- 不需要修改内核或重新编译

应用场景：
- 性能分析
- 网络过滤
- 安全监控
- 故障诊断
```

### 6.2 BCC工具集

**安装BCC：**
```bash
# Ubuntu/Debian
sudo apt install bpfcc-tools linux-headers-$(uname -r)

# 工具位置：/usr/share/bcc/tools/
```

**常用BCC工具：**

```bash
# 1. execsnoop - 追踪新进程
sudo execsnoop-bpfcc
# 输出每个exec系统调用

# 2. opensnoop - 追踪open系统调用
sudo opensnoop-bpfcc
# 查看哪些文件被打开

# 3. biolatency - 块IO延迟分布
sudo biolatency-bpfcc 1
# 输出IO延迟直方图

# 4. biotop - 块IO top
sudo biotop-bpfcc
# 类似iotop，但使用eBPF

# 5. tcplife - TCP会话生命周期
sudo tcplife-bpfcc
# 显示每个TCP连接的生命周期

# 6. tcpretrans - TCP重传追踪
sudo tcpretrans-bpfcc
# 追踪TCP重传包

# 7. runqlat - 运行队列延迟
sudo runqlat-bpfcc 1
# CPU调度器延迟分布

# 8. profile - CPU性能分析
sudo profile-bpfcc -F 99 30
# 类似perf，但开销更小
```

**自定义eBPF程序：**

```python
#!/usr/bin/python3
from bcc import BPF

# eBPF程序（C代码）
prog = """
#include <uapi/linux/ptrace.h>

BPF_HISTOGRAM(dist);

int count(struct pt_regs *ctx) {
    u64 val = bpf_ktime_get_ns() / 1000;  // 微秒
    dist.increment(bpf_log2l(val));
    return 0;
}
"""

# 加载eBPF程序
b = BPF(text=prog)
b.attach_kprobe(event="vfs_read", fn_name="count")

print("Tracing vfs_read latency... Hit Ctrl-C to end.")

try:
    sleep(10)
except KeyboardInterrupt:
    print()

b["dist"].print_log2_hist("usecs")
```

### 6.3 bpftrace

**安装：**
```bash
sudo apt install bpftrace
```

**一行命令强大追踪：**

```bash
# 追踪所有系统调用
sudo bpftrace -e 'tracepoint:raw_syscalls:sys_enter { @[comm] = count(); }'

# 追踪vfs_read延迟
sudo bpftrace -e 'kprobe:vfs_read { @start[tid] = nsecs; }
                   kretprobe:vfs_read /@start[tid]/ {
                     @ns = hist(nsecs - @start[tid]);
                     delete(@start[tid]);
                   }'

# 追踪TCP connect
sudo bpftrace -e 'tracepoint:syscalls:sys_enter_connect {
                     @[comm, args->uservaddr->sa_family] = count();
                   }'

# 追踪malloc分配
sudo bpftrace -e 'uprobe:/lib/x86_64-linux-gnu/libc.so.6:malloc {
                     @[ustack, comm] = count();
                   }'

# 追踪磁盘IO
sudo bpftrace -e 'tracepoint:block:block_rq_issue {
                     @bytes = hist(args->bytes);
                   }'
```

---

## 7. 性能监控体系

### 7.1 监控金字塔

```
监控层次：

┌─────────────────────────────────────┐
│ Business Metrics (业务指标)         │
│ - 订单量                             │
│ - 响应时间                           │
│ - 错误率                             │
└──────────────┬──────────────────────┘
               ↓
┌─────────────────────────────────────┐
│ Application Metrics (应用指标)      │
│ - QPS, TPS                          │
│ - API延迟分布                        │
│ - Cache命中率                        │
└──────────────┬──────────────────────┘
               ↓
┌─────────────────────────────────────┐
│ System Metrics (系统指标)           │
│ - CPU, Memory, Disk, Network        │
│ - 进程、线程状态                     │
│ - 系统调用统计                       │
└──────────────┬──────────────────────┘
               ↓
┌─────────────────────────────────────┐
│ Infrastructure (基础设施)           │
│ - 硬件状态                           │
│ - 网络拓扑                           │
│ - 存储容量                           │
└─────────────────────────────────────┘
```

### 7.2 Prometheus监控栈

**安装Prometheus：**
```bash
# 下载
wget https://github.com/prometheus/prometheus/releases/download/v2.40.0/prometheus-2.40.0.linux-amd64.tar.gz
tar xvfz prometheus-*.tar.gz
cd prometheus-*

# 配置prometheus.yml
cat > prometheus.yml <<EOF
global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'node'
    static_configs:
      - targets: ['localhost:9100']
EOF

# 启动
./prometheus --config.file=prometheus.yml
```

**Node Exporter（系统指标）：**
```bash
# 下载
wget https://github.com/prometheus/node_exporter/releases/download/v1.5.0/node_exporter-1.5.0.linux-amd64.tar.gz
tar xvfz node_exporter-*.tar.gz
cd node_exporter-*

# 启动
./node_exporter

# 访问指标
curl http://localhost:9100/metrics

# 关键指标：
# - node_cpu_seconds_total: CPU时间
# - node_memory_MemAvailable_bytes: 可用内存
# - node_disk_io_time_seconds_total: 磁盘IO时间
# - node_network_receive_bytes_total: 网络接收字节
```

### 7.3 Grafana可视化

**安装Grafana：**
```bash
sudo apt install -y software-properties-common
sudo add-apt-repository "deb https://packages.grafana.com/oss/deb stable main"
sudo apt update
sudo apt install grafana

sudo systemctl start grafana-server
sudo systemctl enable grafana-server

# 访问 http://localhost:3000
# 默认用户名/密码: admin/admin
```

**常用Dashboard：**
- Node Exporter Full (ID: 1860)
- Prometheus 2.0 Stats (ID: 3662)
- Linux系统监控 (ID: 8919)

```bash
# 导入dashboard
# 1. 访问Grafana
# 2. 左侧菜单 -> Dashboards -> Import
# 3. 输入Dashboard ID
# 4. 选择Prometheus数据源
```

---

## 8. 实战调试案例

### 案例1：CPU 100%问题排查

**问题描述**：服务器CPU持续100%

**排查步骤：**

```bash
# 1. 确认是哪个进程
top
# 发现process_x占用98% CPU

# 2. 查看线程级别
top -H -p <pid>
# 发现thread_12345占用最多

# 3. 转换线程ID为16进制
printf "%x\n" 12345
# 输出：3039

# 4. 查看线程栈
sudo jstack <pid> | grep -A 20 3039
# 或对于C++程序
sudo gdb -p <pid>
(gdb) info threads
(gdb) thread 12345
(gdb) bt

# 5. perf分析热点
sudo perf record -g -p <pid> -- sleep 30
sudo perf report

# 6. 生成火焰图
sudo perf record -F 99 -p <pid> -g -- sleep 30
sudo perf script | flamegraph.pl > flame.svg
```

### 案例2：磁盘IO慢排查

**问题描述**：数据库查询变慢

**排查步骤：**

```bash
# 1. 查看磁盘IO情况
iostat -x 1
# 发现sda的await达到50ms，%util 95%

# 2. 查看是哪个进程
sudo iotop -o
# 发现mysqld写入量很大

# 3. 使用blktrace分析
sudo blktrace -d /dev/sda -o trace -w 30
blkparse -i trace -d latency.bin
# 发现大量小IO写入

# 4. 检查MySQL配置
mysql> SHOW VARIABLES LIKE 'innodb_flush%';
# innodb_flush_log_at_trx_commit = 1 (每次事务都刷盘)

# 5. 优化方案
# - 调整为2（每秒刷一次）
# - 增大innodb_log_file_size
# - 使用SSD
# - 启用group commit
```

### 案例3：网络延迟高排查

**问题描述**：API响应时间从10ms增加到100ms

**排查步骤：**

```bash
# 1. 检查网络延迟
ping <server_ip>
# RTT正常

# 2. 检查TCP连接
ss -tin state established
# 发现大量连接处于CLOSE_WAIT

# 3. 检查重传
sudo tcpretrans-bpfcc
# 发现大量重传

# 4. 检查网络丢包
netstat -s | grep -i retrans
# Tcp重传率高

# 5. 抓包分析
sudo tcpdump -i eth0 -w capture.pcap host <server_ip>
# 使用wireshark分析
# 发现：窗口满、接收方处理慢

# 6. 优化方案
# - 增大TCP接收窗口
sudo sysctl -w net.ipv4.tcp_rmem="4096 87380 16777216"
# - 启用TCP窗口缩放
sudo sysctl -w net.ipv4.tcp_window_scaling=1
# - 应用层优化：使用连接池，批量处理
```

### 案例4：内存泄漏排查

**问题描述**：进程内存持续增长

**排查步骤：**

```bash
# 1. 确认内存增长
ps aux | grep process_name
# RSS持续增长

# 2. 使用valgrind检测（开发环境）
valgrind --leak-check=full --log-file=leak.log ./my_program

# 3. 使用heaptrack（生产环境）
heaptrack ./my_program
heaptrack_gui heaptrack.my_program.*.gz

# 4. 使用gdb动态分析
sudo gdb -p <pid>
(gdb) call malloc_stats()
# 查看堆统计

# 5. 使用pmap查看内存映射
pmap -x <pid>
# 找到增长的内存区域

# 6. 分析结果
# 发现：某个hashmap持续增长未清理
# 解决：添加定期清理逻辑
```

---

## 总结

今天我们学习了性能分析的完整工具链：

1. ✅ **性能分析全景**：USE方法、工具分类、分析流程
2. ✅ **CPU分析**：perf、火焰图、mpstat、pidstat
3. ✅ **内存分析**：valgrind、heaptrack、cachegrind
4. ✅ **磁盘IO分析**：iostat、iotop、blktrace、fio
5. ✅ **网络IO分析**：ss、tcpdump、iperf3
6. ✅ **eBPF追踪**：BCC工具集、bpftrace
7. ✅ **监控体系**：Prometheus、Grafana
8. ✅ **实战案例**：CPU、磁盘、网络、内存问题排查

**关键工具速查表：**

```bash
# 快速诊断
vmstat 1          # 系统整体
iostat -x 1       # 磁盘IO
mpstat -P ALL 1   # CPU
free -h           # 内存
ss -s             # 网络

# 深度分析
perf record -g    # CPU profiling
iotop -o          # 进程IO
tcpdump           # 网络抓包
valgrind          # 内存检测

# 现代追踪
bpftrace          # 强大的追踪脚本
execsnoop-bpfcc   # 进程追踪
biolatency-bpfcc  # IO延迟
tcplife-bpfcc     # TCP会话
```

**下一步学习**：
- 课程07: 实战案例分析（LevelDB、Redis、Nginx等）

**推荐资源：**
- 书籍：《性能之巅》（Systems Performance by Brendan Gregg）
- 网站：https://www.brendangregg.com/
- 工具：https://github.com/brendangregg/perf-tools
- eBPF：https://ebpf.io/

性能分析是一门实践的艺术，多用工具、多分析、多思考！🎉

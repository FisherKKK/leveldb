# IO优化课程-05: 高级优化技术与并发控制

## 🎯 课程目标

掌握高性能系统的核心技术：
- 无锁编程与原子操作
- 内存序与内存屏障
- 高级并发控制技术（RCU、Seqlock）
- 批量处理与流水线技术
- 预取、推测执行与分支优化
- 性能优化中的权衡与陷阱

---

## 目录

1. [无锁编程基础](#1-无锁编程基础)
2. [内存序与一致性模型](#2-内存序与一致性模型)
3. [高级并发数据结构](#3-高级并发数据结构)
4. [批量处理技术](#4-批量处理技术)
5. [预取与推测执行](#5-预取与推测执行)
6. [性能优化陷阱](#6-性能优化陷阱)

---

## 1. 无锁编程基础

### 1.1 为什么需要无锁编程

**锁的开销：**

```cpp
// 传统互斥锁
std::mutex mtx;
int counter = 0;

void increment() {
    mtx.lock();        // ~25-50ns (无竞争)
    counter++;         // ~1ns
    mtx.unlock();      // ~25-50ns
}

// 总开销：~50-100ns，其中98%是锁开销
// 高竞争下：锁开销可达微秒级（上下文切换）
```

**锁的问题：**
1. **性能开销**：加锁/解锁、上下文切换
2. **优先级反转**：低优先级线程持有锁
3. **死锁**：多个锁的循环依赖
4. **可扩展性差**：串行化访问

**无锁编程的优势：**
- 无阻塞：线程不会因等待锁而挂起
- 可扩展：多核性能线性增长
- 避免死锁：没有锁就没有死锁
- 低延迟：避免上下文切换

### 1.2 原子操作

```cpp
#include <atomic>

// C++11原子类型
std::atomic<int> counter{0};

// 原子操作（硬件级别）
void atomic_increment() {
    counter.fetch_add(1);  // 单条LOCK XADD指令（x86）
}

// 常用原子操作
std::atomic<int> value{10};

value.load();                      // 原子读取
value.store(20);                   // 原子写入
value.fetch_add(5);                // 原子加法，返回旧值
value.fetch_sub(3);                // 原子减法
value.fetch_and(0xFF);             // 原子AND
value.fetch_or(0x10);              // 原子OR
value.exchange(30);                // 原子交换，返回旧值

// CAS（Compare-And-Swap）
int expected = 10;
bool success = value.compare_exchange_weak(expected, 20);
// 如果value==expected，则value=20，返回true
// 否则expected=value，返回false
```

**CAS的应用：无锁栈**

```cpp
template<typename T>
class LockFreeStack {
    struct Node {
        T data;
        Node* next;
        Node(const T& d) : data(d), next(nullptr) {}
    };
    
    std::atomic<Node*> head{nullptr};
    
public:
    void push(const T& data) {
        Node* new_node = new Node(data);
        new_node->next = head.load();
        
        // CAS循环：如果head没变，设置为new_node
        while (!head.compare_exchange_weak(new_node->next, new_node)) {
            // head改变了，重试
        }
    }
    
    bool pop(T& result) {
        Node* old_head = head.load();
        
        while (old_head && 
               !head.compare_exchange_weak(old_head, old_head->next)) {
            // head改变了，重试
        }
        
        if (old_head) {
            result = old_head->data;
            delete old_head;  // 注意：这里有ABA问题
            return true;
        }
        return false;
    }
};
```

### 1.3 ABA问题与解决

**ABA问题：**

```
时刻1：线程1读取head = A
时刻2：线程2: pop A, pop B, push A  （head: A→B→A）
时刻3：线程1执行CAS，发现head仍是A，CAS成功
问题：A已经不是原来的A了，可能导致错误
```

**解决方案1：引用计数+版本号**

```cpp
template<typename T>
class LockFreeStack {
    struct Node {
        T data;
        Node* next;
    };
    
    struct CountedNodePtr {
        int external_count;
        Node* ptr;
    };
    
    std::atomic<CountedNodePtr> head;
    
public:
    void push(const T& data) {
        CountedNodePtr new_head;
        new_head.ptr = new Node{data};
        new_head.external_count = 1;
        
        new_head.ptr->next = head.load().ptr;
        while (!head.compare_exchange_weak(new_head.ptr->next, new_head.ptr));
    }
    
    // 带版本号的pop，解决ABA问题
};
```

**解决方案2：Hazard Pointer**

```cpp
// 危险指针：标记正在使用的节点，延迟释放
class HazardPointer {
    std::atomic<void*> pointer{nullptr};
    
public:
    void set(void* p) { pointer.store(p); }
    void clear() { pointer.store(nullptr); }
    void* get() { return pointer.load(); }
};

std::array<HazardPointer, MAX_THREADS> hazard_pointers;

// 使用时
void* ptr = hazard_pointers[thread_id].get();
// ... 使用ptr ...
hazard_pointers[thread_id].clear();

// 释放时检查是否有线程在使用
void retire_node(Node* node) {
    for (auto& hp : hazard_pointers) {
        if (hp.get() == node) {
            // 有线程在使用，延迟释放
            retire_list.push_back(node);
            return;
        }
    }
    delete node;  // 安全释放
}
```

---

## 2. 内存序与一致性模型

### 2.1 CPU缓存一致性

**多核CPU的内存视图：**

```
CPU0                     CPU1
┌─────────┐             ┌─────────┐
│ L1 Cache│             │ L1 Cache│
│ x=0     │             │ x=0     │
└────┬────┘             └────┬────┘
     │                       │
     └───────┐       ┐───────┘
             ↓       ↓
      ┌──────────────────┐
      │   L3 Cache (共享)│
      │   x=0            │
      └─────────┬────────┘
                ↓
      ┌──────────────────┐
      │   主内存          │
      │   x=0            │
      └──────────────────┘

问题：CPU0写入x=1后，CPU1何时能看到？
答案：取决于缓存一致性协议（MESI等）和内存屏障
```

**MESI协议（缓存一致性）：**

- **M (Modified)**：独占且已修改
- **E (Exclusive)**：独占且未修改
- **S (Shared)**：共享，只读
- **I (Invalid)**：无效

```
场景：CPU0写入x=1

1. CPU0发送Invalidate消息到其他CPU
2. CPU1收到后标记x为Invalid
3. CPU0等待所有Invalidate确认（可能延迟）
4. CPU0完成写入
5. CPU1下次读取x时，发现Invalid，从CPU0的Cache读取

关键：步骤3可能需要几十到上百个时钟周期
```

### 2.2 内存序（Memory Order）

**C++11内存序：**

```cpp
enum memory_order {
    memory_order_relaxed,   // 最弱：无同步，仅保证原子性
    memory_order_consume,   // 依赖排序（很少用）
    memory_order_acquire,   // 获取：后续读写不会重排到此之前
    memory_order_release,   // 释放：之前读写不会重排到此之后
    memory_order_acq_rel,   // 获取+释放
    memory_order_seq_cst    // 顺序一致（默认，最强）
};
```

**实例：生产者-消费者**

```cpp
// 错误的relaxed用法
std::atomic<int> data{0};
std::atomic<bool> ready{false};

// 生产者
void producer() {
    data.store(42, std::memory_order_relaxed);
    ready.store(true, std::memory_order_relaxed);
    // 问题：这两行可能被重排，消费者可能看到ready=true但data=0
}

// 消费者
void consumer() {
    while (!ready.load(std::memory_order_relaxed));
    int value = data.load(std::memory_order_relaxed);
    // value可能不是42！
}

// 正确的acquire-release用法
void producer_correct() {
    data.store(42, std::memory_order_relaxed);
    ready.store(true, std::memory_order_release);  // 释放：之前的写不会重排到此之后
}

void consumer_correct() {
    while (!ready.load(std::memory_order_acquire));  // 获取：之后的读不会重排到此之前
    int value = data.load(std::memory_order_relaxed);
    assert(value == 42);  // 保证成立
}
```

**性能差异：**

```cpp
// x86-64平台性能测试
std::atomic<int> counter{0};

// 顺序一致（默认）
counter.fetch_add(1);  // ~20ns
// x86: LOCK XADD + MFENCE（全内存屏障）

// relaxed
counter.fetch_add(1, std::memory_order_relaxed);  // ~5ns
// x86: LOCK XADD（无额外屏障）

// acquire-release
counter.fetch_add(1, std::memory_order_acq_rel);  // ~10ns
// x86: LOCK XADD（LOCK前缀隐含屏障）
```

### 2.3 内存屏障

**内存屏障类型：**

1. **LoadLoad屏障**：Load1; LoadLoad; Load2
   - Load2及后续读取不会重排到Load1之前

2. **StoreStore屏障**：Store1; StoreStore; Store2
   - Store2及后续写入不会重排到Store1之前

3. **LoadStore屏障**：Load1; LoadStore; Store2
   - Store2及后续写入不会重排到Load1之前

4. **StoreLoad屏障**：Store1; StoreLoad; Load2
   - Load2及后续读取不会重排到Store1之前（最昂贵）

**汇编示例（x86-64）：**

```cpp
// C++标准屏障
std::atomic_thread_fence(std::memory_order_acquire);
std::atomic_thread_fence(std::memory_order_release);
std::atomic_thread_fence(std::memory_order_seq_cst);

// x86汇编
asm volatile("mfence" ::: "memory");  // 全屏障
asm volatile("lfence" ::: "memory");  // Load屏障
asm volatile("sfence" ::: "memory");  // Store屏障
```

**LevelDB中的内存屏障：**

```cpp
// port/port_stdcxx.h
inline void MemoryBarrier() {
    std::atomic_thread_fence(std::memory_order_seq_cst);
}

// db/skiplist.h - Skip List的无锁读取
template <typename Key, class Comparator>
typename SkipList<Key, Comparator>::Node*
SkipList<Key, Comparator>::FindGreaterOrEqual(const Key& key) const {
    Node* x = head_;
    int level = GetMaxHeight() - 1;
    while (true) {
        Node* next = x->Next(level);
        if (next != nullptr && comparator_(next->key, key) < 0) {
            x = next;
        } else {
            if (level == 0) {
                return next;
            } else {
                level--;
            }
        }
    }
}

// Next()使用acquire语义
Node* Next(int n) {
    return next_[n].load(std::memory_order_acquire);
}

// SetNext()使用release语义
void SetNext(int n, Node* x) {
    next_[n].store(x, std::memory_order_release);
}
```

---

## 3. 高级并发数据结构

### 3.1 RCU（Read-Copy-Update）

**原理：读者无锁，写者拷贝-更新-延迟释放**

```cpp
// RCU示例：无锁读取配置
template<typename T>
class RCUPtr {
    std::atomic<T*> ptr{nullptr};
    
public:
    // 读取：完全无锁
    class ReadGuard {
        T* data;
    public:
        ReadGuard(std::atomic<T*>& p) : data(p.load(std::memory_order_acquire)) {}
        T* operator->() const { return data; }
        T& operator*() const { return *data; }
    };
    
    ReadGuard read() { return ReadGuard(ptr); }
    
    // 更新：拷贝、修改、交换
    void update(std::function<void(T&)> modifier) {
        T* old_ptr = ptr.load();
        T* new_ptr = new T(*old_ptr);  // 拷贝
        modifier(*new_ptr);              // 修改
        
        ptr.store(new_ptr, std::memory_order_release);  // 发布新版本
        
        // 延迟释放old_ptr（确保没有读者在使用）
        synchronize_rcu();  // 等待所有读者退出
        delete old_ptr;
    }
    
private:
    void synchronize_rcu() {
        // 简化版：等待所有线程完成一次调度
        // 实际实现更复杂（Epoch-based reclamation）
        std::this_thread::sleep_for(std::chrono::milliseconds(10));
    }
};

// 使用示例
RCUPtr<Config> config;

// 读者线程（无锁！）
void reader() {
    auto guard = config.read();
    int value = guard->some_value;  // 完全无锁读取
}

// 写者线程
void writer() {
    config.update([](Config& c) {
        c.some_value = 42;
    });
}
```

**RCU的优势：**
- 读者零开销：无原子操作、无内存屏障
- 适合读多写少场景
- Linux内核广泛使用（文件系统、网络栈）

### 3.2 Seqlock（顺序锁）

**适用场景：读者可以容忍偶尔读到不一致数据**

```cpp
class Seqlock {
    std::atomic<uint64_t> seq{0};
    
public:
    // 写者
    void write_lock() {
        uint64_t s;
        do {
            s = seq.load(std::memory_order_relaxed);
        } while (s & 1 || !seq.compare_exchange_weak(s, s + 1));
    }
    
    void write_unlock() {
        seq.fetch_add(1, std::memory_order_release);
    }
    
    // 读者
    uint64_t read_begin() {
        uint64_t s;
        do {
            s = seq.load(std::memory_order_acquire);
        } while (s & 1);  // 等待写者完成
        return s;
    }
    
    bool read_retry(uint64_t start) {
        std::atomic_thread_fence(std::memory_order_acquire);
        return seq.load(std::memory_order_relaxed) != start;
    }
};

// 使用示例
struct Data {
    int x, y;
    Seqlock lock;
};

Data data{{0, 0}};

// 写者
void writer() {
    data.lock.write_lock();
    data.x = 1;
    data.y = 2;
    data.lock.write_unlock();
}

// 读者
void reader() {
    int x, y;
    uint64_t seq;
    do {
        seq = data.lock.read_begin();
        x = data.x;
        y = data.y;
    } while (data.lock.read_retry(seq));  // 检测到冲突则重试
    
    // 现在x和y是一致的
}
```

**Seqlock vs RCU：**
- Seqlock：读者可能需要重试，适合小数据结构
- RCU：读者永不重试，适合大数据结构（指针切换）

### 3.3 无锁队列

**MPSC队列（Multi-Producer, Single-Consumer）：**

```cpp
template<typename T>
class MPSCQueue {
    struct Node {
        std::atomic<Node*> next{nullptr};
        T data;
        Node(const T& d) : data(d) {}
    };
    
    std::atomic<Node*> head;  // 生产者操作
    Node* tail;               // 消费者操作（单线程，无需原子）
    
public:
    MPSCQueue() {
        Node* dummy = new Node(T{});
        head.store(dummy);
        tail = dummy;
    }
    
    // 多个生产者可以并发调用
    void push(const T& data) {
        Node* new_node = new Node(data);
        Node* prev_head = head.exchange(new_node, std::memory_order_acq_rel);
        prev_head->next.store(new_node, std::memory_order_release);
    }
    
    // 只有一个消费者调用
    bool pop(T& result) {
        Node* tail_next = tail->next.load(std::memory_order_acquire);
        if (tail_next == nullptr) {
            return false;  // 队列为空
        }
        
        result = tail_next->data;
        delete tail;
        tail = tail_next;
        return true;
    }
};
```

---

## 4. 批量处理技术

### 4.1 批量处理的原理

**单个处理 vs 批量处理：**

```
单个处理：
for (int i = 0; i < 1000; i++) {
    system_call();      // ~100ns × 1000 = 100μs
    process_one_item();
}

批量处理：
batch items (size=100)
for (int batch_id = 0; batch_id < 10; batch_id++) {
    system_call_batch(100);  // ~500ns × 10 = 5μs
    for (int i = 0; i < 100; i++) {
        process_one_item();
    }
}

性能提升：20倍（100μs → 5μs）
```

### 4.2 批量IO

```cpp
// sendmmsg: 批量发送
struct mmsghdr msgs[BATCH_SIZE];
for (int i = 0; i < BATCH_SIZE; i++) {
    // 设置msgs[i]
}
int sent = sendmmsg(sockfd, msgs, BATCH_SIZE, 0);

// io_uring: 批量提交
struct io_uring ring;
io_uring_queue_init(256, &ring, 0);

for (int i = 0; i < BATCH_SIZE; i++) {
    struct io_uring_sqe *sqe = io_uring_get_sqe(&ring);
    io_uring_prep_read(sqe, fds[i], bufs[i], sizes[i], 0);
}
io_uring_submit(&ring);  // 一次系统调用提交所有IO

// 批量收割
struct io_uring_cqe *cqe;
while (io_uring_peek_cqe(&ring, &cqe) == 0) {
    process_completion(cqe);
    io_uring_cqe_seen(&ring, cqe);
}
```

### 4.3 批量内存分配

```cpp
// Arena分配器：批量分配，统一释放
class Arena {
    std::vector<char*> blocks;
    char* alloc_ptr = nullptr;
    size_t alloc_bytes_remaining = 0;
    static const size_t kBlockSize = 4096;
    
public:
    char* Allocate(size_t bytes) {
        if (bytes <= alloc_bytes_remaining) {
            char* result = alloc_ptr;
            alloc_ptr += bytes;
            alloc_bytes_remaining -= bytes;
            return result;
        }
        return AllocateFallback(bytes);
    }
    
    char* AllocateFallback(size_t bytes) {
        if (bytes > kBlockSize / 4) {
            // 大对象单独分配
            return AllocateNewBlock(bytes);
        }
        
        // 分配新block
        alloc_ptr = AllocateNewBlock(kBlockSize);
        alloc_bytes_remaining = kBlockSize;
        
        char* result = alloc_ptr;
        alloc_ptr += bytes;
        alloc_bytes_remaining -= bytes;
        return result;
    }
    
    char* AllocateNewBlock(size_t block_bytes) {
        char* result = new char[block_bytes];
        blocks.push_back(result);
        return result;
    }
    
    ~Arena() {
        for (char* block : blocks) {
            delete[] block;
        }
    }
};

// LevelDB的Skip List使用Arena
// 减少malloc调用：1000次插入，1次malloc vs 1000次malloc
```

---

## 5. 预取与推测执行

### 5.1 软件预取

```cpp
#include <xmmintrin.h>  // SSE指令

// 预取数据到Cache
void prefetch_example(int* data, int n) {
    for (int i = 0; i < n; i++) {
        // 预取下一次迭代的数据
        if (i + 8 < n) {
            _mm_prefetch((const char*)&data[i + 8], _MM_HINT_T0);
            // T0: L1 Cache
            // T1: L2 Cache
            // T2: L3 Cache
            // NTA: Non-temporal (绕过Cache)
        }
        
        // 处理当前数据
        process(data[i]);
    }
}

// 链表预取（更有效）
struct Node {
    int data;
    Node* next;
};

void traverse_with_prefetch(Node* head) {
    Node* curr = head;
    Node* next = curr ? curr->next : nullptr;
    
    while (curr) {
        if (next) {
            _mm_prefetch((const char*)next, _MM_HINT_T0);
            if (next->next) {
                _mm_prefetch((const char*)next->next, _MM_HINT_T0);
            }
        }
        
        process(curr->data);
        curr = next;
        next = curr ? curr->next : nullptr;
    }
}
```

**预取效果：**
- 顺序访问：硬件预取已经很好，软件预取收益小
- 随机访问：软件预取可提升50-200%
- 链表遍历：预取下2-3个节点效果最佳

### 5.2 分支预测优化

```cpp
// likely/unlikely宏（GCC）
#define likely(x)   __builtin_expect(!!(x), 1)
#define unlikely(x) __builtin_expect(!!(x), 0)

// 提示编译器分支概率
if (likely(status.ok())) {
    // 热路径：编译器会优化为顺序执行
    normal_processing();
} else {
    // 冷路径：可能被放到函数末尾
    error_handling();
}

// 避免分支：使用CMOV
// 不好：有分支
int max_branchy(int a, int b) {
    return a > b ? a : b;
}

// 更好：无分支（现代编译器会自动优化）
int max_branchless(int a, int b) {
    return a > b ? a : b;  // 编译为CMOV指令
}

// 手动无分支化
int abs_branchless(int x) {
    int mask = x >> 31;      // x < 0 ? 0xFFFFFFFF : 0x00000000
    return (x + mask) ^ mask;
}
```

**分支预测失败的代价：**
- 现代CPU：10-20个时钟周期
- 流水线flush
- 推测执行的结果被丢弃

**优化策略：**
1. 消除分支（CMOV、查表）
2. 提示分支概率（likely/unlikely）
3. 重组代码（热路径连续）

---

## 6. 性能优化陷阱

### 6.1 False Sharing

```cpp
// 错误：false sharing
struct Counter {
    std::atomic<int> count{0};
    char padding[60];  // 假设Cache Line是64字节
};

Counter counters[NUM_THREADS];  // 每个线程一个计数器

void worker(int thread_id) {
    for (int i = 0; i < ITERATIONS; i++) {
        counters[thread_id].count.fetch_add(1);
    }
}

// 问题：如果counters[0]和counters[1]在同一Cache Line
// 两个线程会不断使对方的Cache Line失效
// 性能下降可达10倍！

// 正确：添加padding或使用alignas
struct alignas(64) Counter {
    std::atomic<int> count{0};
};
```

**检测False Sharing：**
```bash
perf c2c record -a -- ./program
perf c2c report
# 查找Cache-to-Cache传输热点
```

### 6.2 过度同步

```cpp
// 错误：过度使用seq_cst
std::atomic<int> flag{0};

void thread1() {
    flag.store(1);  // 默认seq_cst，开销大
}

void thread2() {
    while (flag.load() == 0);  // 默认seq_cst，持续刷新Cache
}

// 正确：使用适当的内存序
void thread1_optimized() {
    flag.store(1, std::memory_order_release);
}

void thread2_optimized() {
    while (flag.load(std::memory_order_acquire) == 0) {
        // 添加退避，减少Cache竞争
        _mm_pause();  // x86: PAUSE指令
    }
}
```

### 6.3 锁粒度错误

```cpp
// 太粗：整个数据结构一把锁
std::mutex global_lock;
std::unordered_map<int, int> map;

void insert(int key, int value) {
    std::lock_guard<std::mutex> lock(global_lock);
    map[key] = value;  // 所有插入串行化
}

// 太细：每个节点一把锁（死锁风险）
struct Node {
    int data;
    Node* next;
    std::mutex lock;
};

// 合适：分段锁
template<int NUM_SHARDS>
class ShardedMap {
    struct Shard {
        std::mutex lock;
        std::unordered_map<int, int> map;
    };
    
    Shard shards[NUM_SHARDS];
    
    Shard& get_shard(int key) {
        return shards[key % NUM_SHARDS];
    }
    
public:
    void insert(int key, int value) {
        Shard& shard = get_shard(key);
        std::lock_guard<std::mutex> lock(shard.lock);
        shard.map[key] = value;
    }
};
```

### 6.4 优化的权衡

**权衡1：复杂度 vs 性能**
```cpp
// 简单但稍慢
std::mutex mtx;
void simple_increment(int& counter) {
    std::lock_guard<std::mutex> lock(mtx);
    counter++;
}

// 复杂但更快
std::atomic<int> atomic_counter{0};
void lock_free_increment() {
    atomic_counter.fetch_add(1, std::memory_order_relaxed);
}

// 何时选择：
// - 低频操作：选简单
// - 热路径：选高效
// - 不确定：先测量
```

**权衡2：内存 vs 速度**
```cpp
// 空间换时间：缓存结果
std::unordered_map<int, int> cache;

int expensive_computation(int x) {
    if (cache.count(x)) {
        return cache[x];  // O(1)
    }
    int result = complex_algorithm(x);  // O(n²)
    cache[x] = result;
    return result;
}

// 何时使用：
// - 计算昂贵 >> 查表开销
// - 重复查询多
// - 内存充足
```

---

## 7. 实战：LevelDB的并发控制

### 7.1 Skip List的无锁读取

```cpp
// db/skiplist.h
// 写入需要外部同步，但读取完全无锁
template <typename Key, class Comparator>
class SkipList {
private:
    struct Node {
        explicit Node(const Key& k) : key(k) {}
        
        Key const key;
        
        // 使用原子指针数组
        std::atomic<Node*> next_[1];
        
        Node* Next(int n) {
            return next_[n].load(std::memory_order_acquire);
        }
        
        void SetNext(int n, Node* x) {
            next_[n].store(x, std::memory_order_release);
        }
    };
    
public:
    // 读取：完全无锁
    bool Contains(const Key& key) const {
        Node* x = FindGreaterOrEqual(key, nullptr);
        if (x != nullptr && Equal(key, x->key)) {
            return true;
        }
        return false;
    }
    
    // 插入：需要外部同步（通常是DBImpl的mutex）
    void Insert(const Key& key) {
        // ...
        x->SetNext(i, new_node);  // release语义
    }
};
```

### 7.2 DBImpl的锁策略

```cpp
// db/db_impl.cc
class DBImpl : public DB {
private:
    mutable port::Mutex mutex_;  // 保护大部分内部状态
    
public:
    Status Get(const ReadOptions& options, const Slice& key, std::string* value) {
        mutex_.Lock();
        
        // 获取当前版本（引用计数）
        Version* current = versions_->current();
        current->Ref();
        
        mutex_.Unlock();
        
        // 无锁读取MemTable和SSTable
        Status s = mem_->Get(key, value, &s);
        if (!s.ok()) {
            s = current->Get(options, key, value, &stats);
        }
        
        mutex_.Lock();
        current->Unref();
        mutex_.Unlock();
        
        return s;
    }
    
    Status Write(const WriteOptions& options, WriteBatch* updates) {
        mutex_.Lock();
        
        // 批量合并写入
        Writer w(&mutex_);
        writers_.push_back(&w);
        
        // ...写入WAL和MemTable...
        
        mutex_.Unlock();
        return status;
    }
};
```

**关键设计：**
1. 读取快路径：最小化锁持有时间
2. 引用计数：允许无锁访问旧版本
3. 批量写入：减少锁竞争

---

## 8. 性能优化清单

```
□ 并发控制
  □ 热路径使用无锁或细粒度锁
  □ 避免false sharing（64字节对齐）
  □ 使用合适的内存序（不滥用seq_cst）
  □ 考虑读写比例（RCU/Seqlock）

□ 批量处理
  □ 批量IO（sendmmsg, io_uring）
  □ 批量内存分配（Arena）
  □ 批量提交（数据库事务）

□ Cache优化
  □ 数据对齐
  □ 软件预取（链表、随机访问）
  □ 减少Cache行竞争

□ 分支优化
  □ likely/unlikely提示
  □ 消除分支（CMOV、查表）
  □ 热代码路径内联

□ 测量与验证
  □ 使用perf分析瓶颈
  □ 避免过早优化
  □ 量化改进效果
```

---

## 9. 总结

**关键要点：**

1. **无锁不是万能**：复杂度高，易出错，仅用于热点
2. **内存序很重要**：错误使用导致难以调试的bug
3. **批量处理**：减少开销的通用方法
4. **测量驱动**：不测量就优化是浪费时间

**学习路径：**
1. 理解底层：Cache一致性、内存序
2. 掌握工具：原子操作、内存屏障
3. 研究案例：Linux内核、LevelDB、Folly
4. 实践验证：实现并测试自己的无锁数据结构

---

**下一课预告：IO优化课程-06-性能分析与调优工具实战**
- perf、flamegraph火焰图
- strace、ltrace系统调用追踪
- valgrind、cachegrind缓存分析
- BPF/eBPF动态追踪
- 自定义性能计数器

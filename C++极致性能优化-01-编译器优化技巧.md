# C++极致性能优化-01: 编译器优化技巧

## 🎯 课程目标

掌握编译器层面的性能优化技术：
- 编译器优化级别与选项
- 函数内联与分支预测
- Link Time Optimization (LTO)
- Profile Guided Optimization (PGO)
- 编译器属性与内建函数
- 目标CPU优化

---

## 目录

1. [编译器优化级别](#1-编译器优化级别)
2. [函数内联优化](#2-函数内联优化)
3. [分支预测优化](#3-分支预测优化)
4. [编译器属性](#4-编译器属性)
5. [LTO链接时优化](#5-lto链接时优化)
6. [PGO性能引导优化](#6-pgo性能引导优化)
7. [目标CPU优化](#7-目标cpu优化)
8. [编译器内建函数](#8-编译器内建函数)

---

## 1. 编译器优化级别

### 1.1 GCC/Clang优化级别对比

```bash
# -O0: 无优化（默认）
g++ -O0 main.cpp -o app
# - 编译速度：最快
# - 调试友好：是
# - 性能：最差
# - 二进制大小：最大

# -O1: 基础优化
g++ -O1 main.cpp -o app
# - 死代码消除
# - 常量折叠
# - 简单内联

# -O2: 推荐的生产级优化
g++ -O2 main.cpp -o app
# - 循环优化
# - 指令调度
# - 寄存器分配优化
# - 通用子表达式消除
# - 性能提升：通常10-30%

# -O3: 激进优化
g++ -O3 main.cpp -o app
# - 循环向量化
# - 函数内联（更激进）
# - 循环展开
# - 性能提升：额外5-15%
# - 风险：代码膨胀、可能降低性能

# -Os: 优化代码大小
g++ -Os main.cpp -o app
# - 适合嵌入式系统
# - 牺牲部分性能换取更小二进制

# -Ofast: 最大速度（非标准）
g++ -Ofast main.cpp -o app
# - 包含-O3
# - -ffast-math（牺牲IEEE浮点精度）
# - 性能提升：额外5-10%
# - 风险：浮点计算可能不精确

# -Og: 调试优化
g++ -Og main.cpp -o app -g
# - 保持调试信息可用
# - 适度优化
# - 适合开发阶段
```

**性能对比示例：**

```cpp
// 测试程序：计算斐波那契数列
#include <iostream>
#include <chrono>

long long fibonacci(int n) {
    if (n <= 1) return n;
    return fibonacci(n - 1) + fibonacci(n - 2);
}

int main() {
    auto start = std::chrono::high_resolution_clock::now();

    long long result = fibonacci(40);

    auto end = std::chrono::high_resolution_clock::now();
    auto duration = std::chrono::duration_cast<std::chrono::milliseconds>(
        end - start).count();

    std::cout << "Result: " << result << "\n";
    std::cout << "Time: " << duration << " ms\n";

    return 0;
}
```

**编译与测试：**

```bash
# 测试不同优化级别
g++ -O0 fib.cpp -o fib_O0 && ./fib_O0
# Time: 1200 ms

g++ -O1 fib.cpp -o fib_O1 && ./fib_O1
# Time: 950 ms (21%提升)

g++ -O2 fib.cpp -o fib_O2 && ./fib_O2
# Time: 850 ms (29%提升)

g++ -O3 fib.cpp -o fib_O3 && ./fib_O3
# Time: 850 ms (与O2相当，此例中无额外提升)

# 结论：O2通常是最佳选择
```

### 1.2 关键优化选项

```bash
# 推荐的生产编译选项
g++ -O3 \
    -march=native \         # 使用本机CPU所有指令集
    -mtune=native \         # 针对本机CPU调优
    -flto \                 # 链接时优化
    -ffast-math \           # 快速数学（牺牲精度）
    -funroll-loops \        # 循环展开
    -finline-functions \    # 函数内联
    -fomit-frame-pointer \  # 省略帧指针（多一个寄存器）
    -fno-exceptions \       # 禁用异常（如不需要）
    -fno-rtti \             # 禁用RTTI（如不需要）
    main.cpp -o app

# 更激进的优化（谨慎使用）
g++ -Ofast \
    -march=native \
    -flto \
    -fno-signed-zeros \     # 假设没有-0.0
    -fno-trapping-math \    # 假设浮点不会陷入
    -fassociative-math \    # 允许重新关联浮点运算
    -freciprocal-math \     # 使用倒数近似
    main.cpp -o app
```

**优化选项说明：**

```cpp
// 1. -ffast-math的影响
float fast_sqrt(float x) {
    return std::sqrt(x);
}

// -ffast-math允许编译器：
// - 假设没有NaN/Inf
// - 重新排序浮点运算
// - 使用近似指令（如rsqrtss）

// 性能提升：2-5倍（某些数学密集型代码）
// 风险：精度损失、边界情况处理不当


// 2. -fno-exceptions的影响
class MyClass {
public:
    MyClass() {
        // 无异常：构造函数更简单
        // 有异常：需要栈展开代码
    }
};

// 禁用异常后：
// - 二进制减小：10-30%
// - 性能提升：5-10%（减少栈展开检查）
// - 必须使用错误码或其他机制


// 3. -fomit-frame-pointer的影响
void function() {
    // 帧指针（rbp/ebp）用于栈回溯
    // 省略后，rbp可作为通用寄存器

    // 性能提升：1-3%
    // 代价：调试困难、栈回溯不可用
}
```

---

## 2. 函数内联优化

### 2.1 内联基础

```cpp
// 自动内联（编译器决定）
inline int add(int a, int b) {
    return a + b;
}

// 强制内联（GCC/Clang）
__attribute__((always_inline))
inline int force_add(int a, int b) {
    return a + b;
}

// 禁止内联
__attribute__((noinline))
int no_inline_add(int a, int b) {
    return a + b;
}

// MSVC语法
__forceinline int msvc_force_add(int a, int b) {
    return a + b;
}
```

**内联的影响：**

```cpp
// 测试：函数调用开销
#include <iostream>
#include <chrono>

// 小函数（适合内联）
inline int small_func(int x) {
    return x * 2 + 1;
}

__attribute__((noinline))
int non_inlined_small(int x) {
    return x * 2 + 1;
}

// 大函数（不适合内联）
inline int large_func(int x) {
    int result = x;
    for (int i = 0; i < 100; i++) {
        result = result * 2 + i;
    }
    return result;
}

void benchmark_inlining() {
    constexpr int N = 100000000;

    // 测试内联小函数
    auto start = std::chrono::high_resolution_clock::now();
    long long sum = 0;
    for (int i = 0; i < N; i++) {
        sum += small_func(i);
    }
    auto end = std::chrono::high_resolution_clock::now();
    auto inlined_time = std::chrono::duration_cast<std::chrono::milliseconds>(
        end - start).count();

    // 测试非内联小函数
    start = std::chrono::high_resolution_clock::now();
    sum = 0;
    for (int i = 0; i < N; i++) {
        sum += non_inlined_small(i);
    }
    end = std::chrono::high_resolution_clock::now();
    auto non_inlined_time = std::chrono::duration_cast<std::chrono::milliseconds>(
        end - start).count();

    std::cout << "Inlined:     " << inlined_time << " ms\n";
    std::cout << "Non-inlined: " << non_inlined_time << " ms\n";
    std::cout << "Speedup:     " << (double)non_inlined_time / inlined_time << "x\n";
}

// 输出示例：
// Inlined:     80 ms
// Non-inlined: 450 ms
// Speedup:     5.6x

// 函数调用开销：~5ns（x86-64）
// 小函数计算：~1ns
// 内联后消除了85%的开销
```

### 2.2 内联决策

```cpp
// 何时应该内联？

// ✓ 适合内联：
// 1. 小函数（<10行代码）
inline int max(int a, int b) {
    return a > b ? a : b;
}

// 2. 频繁调用的函数
inline int is_digit(char c) {
    return c >= '0' && c <= '9';
}

// 3. 模板函数
template<typename T>
inline T square(T x) {
    return x * x;
}


// ✗ 不适合内联：
// 1. 大函数（>50行代码）
void complex_algorithm() {
    // 数百行代码...
}

// 2. 递归函数
int factorial(int n) {
    if (n <= 1) return 1;
    return n * factorial(n - 1);  // 递归，无法内联
}

// 3. 虚函数（动态绑定）
class Base {
public:
    virtual void process() {}  // 通常不能内联
};

// 4. 通过函数指针调用
void (*func_ptr)(int) = &some_function;  // 无法内联


// 编译器内联启发式规则：
// - 函数大小 < 阈值（通常30-50个指令）
// - 调用频率高
// - 无递归
// - 无间接调用
// - 考虑代码膨胀vs性能权衡
```

**内联控制选项：**

```bash
# 控制内联阈值
g++ -O2 \
    -finline-limit=100 \        # 内联函数大小限制
    -finline-functions \        # 内联所有合适的函数
    -finline-small-functions \  # 内联小函数
    main.cpp -o app

# 查看内联决策
g++ -O2 -Rpass=inline main.cpp
# 输出：函数xxx已被内联到yyy
```

---

## 3. 分支预测优化

### 3.1 分支预测提示

```cpp
// GCC/Clang分支预测提示
#define likely(x)   __builtin_expect(!!(x), 1)
#define unlikely(x) __builtin_expect(!!(x), 0)

// 使用示例：
void process_data(int* data, int size) {
    for (int i = 0; i < size; i++) {
        // 提示编译器：这个条件很少为真
        if (unlikely(data[i] < 0)) {
            handle_error(data[i]);
        }

        // 正常处理（热路径）
        data[i] *= 2;
    }
}

// 网络包处理示例
void handle_packet(Packet* pkt) {
    // 提示：正常包很常见
    if (likely(pkt->is_valid())) {
        process_normal(pkt);
    } else {
        // 错误包很罕见
        handle_corrupt(pkt);
    }
}
```

**分支预测的影响：**

```cpp
#include <iostream>
#include <chrono>
#include <cstdlib>

#define likely(x)   __builtin_expect(!!(x), 1)
#define unlikely(x) __builtin_expect(!!(x), 0)

// 测试：分支预测失效的代价
void benchmark_branch_prediction() {
    constexpr int N = 100000000;
    int* data = new int[N];

    // 生成随机数据（50%正，50%负）
    for (int i = 0; i < N; i++) {
        data[i] = rand() - RAND_MAX / 2;
    }

    // 测试1：无提示
    auto start = std::chrono::high_resolution_clock::now();
    long long sum1 = 0;
    for (int i = 0; i < N; i++) {
        if (data[i] > 0) {
            sum1 += data[i];
        }
    }
    auto end = std::chrono::high_resolution_clock::now();
    auto time1 = std::chrono::duration_cast<std::chrono::milliseconds>(
        end - start).count();

    // 测试2：使用likely提示（错误提示！）
    start = std::chrono::high_resolution_clock::now();
    long long sum2 = 0;
    for (int i = 0; i < N; i++) {
        if (likely(data[i] > 0)) {  // 错误：实际50/50
            sum2 += data[i];
        }
    }
    end = std::chrono::high_resolution_clock::now();
    auto time2 = std::chrono::duration_cast<std::chrono::milliseconds>(
        end - start).count();

    // 生成偏斜数据（95%正，5%负）
    for (int i = 0; i < N; i++) {
        data[i] = (rand() % 100 < 95) ? 1 : -1;
    }

    // 测试3：正确使用likely
    start = std::chrono::high_resolution_clock::now();
    long long sum3 = 0;
    for (int i = 0; i < N; i++) {
        if (likely(data[i] > 0)) {  // 正确：95%为真
            sum3 += data[i];
        }
    }
    end = std::chrono::high_resolution_clock::now();
    auto time3 = std::chrono::duration_cast<std::chrono::milliseconds>(
        end - start).count();

    std::cout << "Random (no hint):  " << time1 << " ms\n";
    std::cout << "Random (likely):   " << time2 << " ms\n";
    std::cout << "Skewed (likely):   " << time3 << " ms\n";

    delete[] data;
}

// 输出示例：
// Random (no hint):  850 ms
// Random (likely):   850 ms (无改善，50/50分支)
// Skewed (likely):   320 ms (62%提升！)

// 分支预测失效代价：~15-20个时钟周期
```

### 3.2 消除分支

```cpp
// 技巧1：使用算术代替分支
// 不好：使用分支
int abs_with_branch(int x) {
    if (x < 0) {
        return -x;
    }
    return x;
}

// 好：无分支版本
int abs_branchless(int x) {
    int mask = x >> 31;  // 全0（正数）或全1（负数）
    return (x ^ mask) - mask;
}


// 技巧2：使用条件移动（CMOV）
// 不好：分支
int max_with_branch(int a, int b) {
    if (a > b) {
        return a;
    }
    return b;
}

// 好：条件移动（编译器会生成CMOV指令）
int max_branchless(int a, int b) {
    return (a > b) ? a : b;  // CMOV指令
}


// 技巧3：查表法
// 不好：多个分支
char to_upper_with_branch(char c) {
    if (c >= 'a' && c <= 'z') {
        return c - 32;
    }
    return c;
}

// 好：查表
static const char upper_table[256] = {
    // 预计算的转换表...
};

char to_upper_table(char c) {
    return upper_table[static_cast<unsigned char>(c)];
}


// 技巧4：位操作技巧
// 判断是否2的幂
bool is_power_of_two_with_branch(int x) {
    if (x <= 0) return false;
    int count = 0;
    while (x > 0) {
        if (x & 1) count++;
        x >>= 1;
    }
    return count == 1;
}

bool is_power_of_two_branchless(int x) {
    return x > 0 && (x & (x - 1)) == 0;
}
```

**性能对比：**

```cpp
void benchmark_branchless() {
    constexpr int N = 100000000;
    int* data = new int[N];

    for (int i = 0; i < N; i++) {
        data[i] = rand() - RAND_MAX / 2;
    }

    // 测试abs：有分支版本
    auto start = std::chrono::high_resolution_clock::now();
    long long sum = 0;
    for (int i = 0; i < N; i++) {
        sum += abs_with_branch(data[i]);
    }
    auto end = std::chrono::high_resolution_clock::now();
    auto branch_time = std::chrono::duration_cast<std::chrono::milliseconds>(
        end - start).count();

    // 测试abs：无分支版本
    start = std::chrono::high_resolution_clock::now();
    sum = 0;
    for (int i = 0; i < N; i++) {
        sum += abs_branchless(data[i]);
    }
    end = std::chrono::high_resolution_clock::now();
    auto branchless_time = std::chrono::duration_cast<std::chrono::milliseconds>(
        end - start).count();

    std::cout << "With branch:    " << branch_time << " ms\n";
    std::cout << "Branchless:     " << branchless_time << " ms\n";
    std::cout << "Speedup:        " << (double)branch_time / branchless_time << "x\n";

    delete[] data;
}

// 输出示例：
// With branch:    650 ms
// Branchless:     280 ms
// Speedup:        2.3x
```

---

## 4. 编译器属性

### 4.1 函数属性

```cpp
// 1. hot/cold属性：优化优先级
__attribute__((hot))
void frequently_called() {
    // 编译器会：
    // - 激进优化
    // - 放在代码段前部（提高cache命中率）
}

__attribute__((cold))
void error_handler() {
    // 编译器会：
    // - 最小优化
    // - 放在代码段末尾
    // - 假设很少执行
}


// 2. pure/const属性：无副作用
__attribute__((pure))
int compute_checksum(const char* data, size_t len) {
    // pure：只读取参数，不修改全局状态
    // 编译器可以消除重复调用
    int sum = 0;
    for (size_t i = 0; i < len; i++) {
        sum += data[i];
    }
    return sum;
}

__attribute__((const))
int square(int x) {
    // const：更严格，连参数都不读取（除了值）
    // 完全无副作用，结果只依赖参数
    return x * x;
}

// 示例：编译器优化
void use_pure() {
    const char* data = "hello";

    int cs1 = compute_checksum(data, 5);
    int cs2 = compute_checksum(data, 5);  // 编译器可能消除此调用

    // 编译器知道结果相同，可能优化为：
    // int cs1 = compute_checksum(data, 5);
    // int cs2 = cs1;
}


// 3. noreturn属性：不返回
__attribute__((noreturn))
void fatal_error(const char* msg) {
    fprintf(stderr, "Fatal: %s\n", msg);
    exit(1);
    // 编译器知道执行不会继续
}

void use_noreturn(int x) {
    if (x < 0) {
        fatal_error("Invalid value");
        // 编译器知道这里不会返回，不会生成后续代码
    }

    // 正常处理...
}


// 4. flatten属性：内联所有被调用函数
__attribute__((flatten))
void process_all() {
    step1();
    step2();
    step3();
    // 所有step函数都会被内联到这里
}


// 5. target属性：特定CPU特性
__attribute__((target("avx2")))
void process_with_avx2(float* data, size_t n) {
    // 可以使用AVX2指令
    // 即使全局编译选项不包含AVX2
}

__attribute__((target("default")))
void process_generic(float* data, size_t n) {
    // 通用版本
}

// 运行时选择：
void process(float* data, size_t n) {
    if (cpu_supports_avx2()) {
        process_with_avx2(data, n);
    } else {
        process_generic(data, n);
    }
}


// 6. malloc属性：分配新内存
__attribute__((malloc))
void* my_alloc(size_t size) {
    return malloc(size);
    // 编译器知道返回的指针不会别名其他指针
}
```

### 4.2 变量属性

```cpp
// 1. aligned属性：指定对齐
__attribute__((aligned(64)))
int cache_line_var;  // 64字节对齐

struct __attribute__((aligned(32))) AlignedStruct {
    float data[8];  // AVX对齐
};


// 2. packed属性：紧凑布局
struct __attribute__((packed)) PackedStruct {
    char a;     // 1 byte
    int b;      // 4 bytes, no padding
    char c;     // 1 byte
};  // sizeof = 6 bytes


// 3. section属性：指定段
__attribute__((section(".hot_data")))
int frequently_accessed = 0;

__attribute__((section(".cold_data")))
int rarely_accessed = 0;


// 4. weak属性：弱符号
__attribute__((weak))
void logging_func() {
    // 默认实现
}

// 用户可以覆盖
void logging_func() {
    // 自定义实现
}


// 5. visibility属性：符号可见性
__attribute__((visibility("hidden")))
void internal_function() {
    // 对外部不可见，允许更好的优化
}

__attribute__((visibility("default")))
void public_api() {
    // 对外可见
}
```

---

## 5. LTO链接时优化

### 5.1 LTO基础

```bash
# 编译时启用LTO
# 方法1：单步编译
g++ -flto -O3 main.cpp lib.cpp -o app

# 方法2：分步编译
g++ -flto -O3 -c main.cpp -o main.o
g++ -flto -O3 -c lib.cpp -o lib.o
g++ -flto -O3 main.o lib.o -o app  # 链接时优化

# LTO的作用：
# - 跨文件内联
# - 全局死代码消除
# - 常量传播
# - 函数特化
```

**LTO示例：**

```cpp
// lib.cpp
int compute_heavy(int x) {
    int result = 0;
    for (int i = 0; i < 1000; i++) {
        result += x * i;
    }
    return result;
}

// main.cpp
extern int compute_heavy(int x);

int main() {
    int result = compute_heavy(42);  // 常量参数
    return result;
}

// 无LTO：
// - compute_heavy是函数调用
// - 参数42通过寄存器传递

// 有LTO：
// - 编译器看到compute_heavy总是用42调用
// - 内联compute_heavy到main
// - 循环中x*i变为42*i，可能进一步优化
// - 可能编译时计算整个结果
```

### 5.2 LTO配置

```bash
# 并行LTO（加速编译）
g++ -flto=8 -O3 main.cpp -o app  # 使用8个线程

# Thin LTO（Clang）：更快的LTO
clang++ -flto=thin -O3 main.cpp -o app

# 查看LTO统计
g++ -flto -O3 -fwhole-program-vtables main.cpp -o app \
    -Wl,--plugin-opt=stats

# LTO性能提升：
# - 小程序：5-15%
# - 大项目：10-30%
# - 编译时间：增加2-5倍
```

---

## 6. PGO性能引导优化

### 6.1 PGO工作流程

```bash
# 步骤1：插桩编译
g++ -fprofile-generate -O2 main.cpp -o app_instrumented

# 步骤2：运行程序，收集性能数据
./app_instrumented < typical_workload.txt
# 生成.gcda文件（性能数据）

# 步骤3：使用性能数据重新编译
g++ -fprofile-use -O2 main.cpp -o app_optimized

# 步骤4：清理
rm -f *.gcda
```

**PGO的优化：**

```cpp
// PGO通过真实运行数据优化：

// 1. 分支预测
void process(int x) {
    if (x > 100) {          // PGO知道这个分支很少执行
        handle_large(x);    // 放到cold段
    } else {
        handle_normal(x);   // 放到hot段
    }
}

// 2. 函数内联决策
void caller() {
    foo();  // PGO知道foo被频繁调用，会内联
    bar();  // PGO知道bar很少调用，不内联
}

// 3. 循环优化
for (int i = 0; i < n; i++) {
    if (data[i] > threshold) {  // PGO知道这个条件通常为假
        process(data[i]);
    }
}
// PGO会优化冷路径
```

### 6.2 PGO实战

```cpp
// 示例：字符串处理
#include <iostream>
#include <string>
#include <chrono>

void process_string(const std::string& str) {
    if (str.length() > 1000) {  // 长字符串（罕见）
        // 复杂处理...
    } else if (str.length() > 100) {  // 中等长度（较少）
        // 中等处理...
    } else {  // 短字符串（常见）
        // 简单处理...
    }
}

int main() {
    // 典型工作负载：大部分是短字符串
    for (int i = 0; i < 1000000; i++) {
        std::string str(10 + rand() % 20, 'x');  // 10-30字符
        process_string(str);
    }
    return 0;
}
```

**编译测试：**

```bash
# 无PGO
g++ -O3 main.cpp -o app_no_pgo
time ./app_no_pgo
# Time: 2.5s

# 有PGO
g++ -fprofile-generate -O3 main.cpp -o app_instr
./app_instr  # 收集数据
g++ -fprofile-use -O3 main.cpp -o app_pgo
time ./app_pgo
# Time: 1.8s（28%提升）

# PGO优化了：
# - 分支预测（短字符串路径热）
# - 代码布局（热代码在一起）
# - 内联决策（基于实际调用频率）
```

---

## 7. 目标CPU优化

### 7.1 CPU特性选择

```bash
# 查看当前CPU特性
gcc -march=native -Q --help=target | grep enabled

# 通用优化选项
g++ -march=x86-64 main.cpp  # 兼容所有x86-64 CPU

# 针对CPU代优化
g++ -march=haswell main.cpp   # Intel Haswell及更新
g++ -march=skylake main.cpp   # Intel Skylake及更新
g++ -march=znver2 main.cpp    # AMD Zen 2

# 本机最优（不可移植）
g++ -march=native main.cpp    # 使用本机所有特性

# 细粒度控制
g++ -msse4.2 -mavx2 -mfma main.cpp
```

**CPU特性影响：**

```cpp
// 示例：向量化求和
#include <immintrin.h>

// 标量版本
float sum_scalar(const float* data, size_t n) {
    float sum = 0;
    for (size_t i = 0; i < n; i++) {
        sum += data[i];
    }
    return sum;
}

// SSE版本（4路并行）
float sum_sse(const float* data, size_t n) {
    __m128 sum_vec = _mm_setzero_ps();

    size_t i = 0;
    for (; i + 4 <= n; i += 4) {
        __m128 v = _mm_loadu_ps(&data[i]);
        sum_vec = _mm_add_ps(sum_vec, v);
    }

    float sum = 0;
    sum += ((float*)&sum_vec)[0];
    sum += ((float*)&sum_vec)[1];
    sum += ((float*)&sum_vec)[2];
    sum += ((float*)&sum_vec)[3];

    // 处理剩余
    for (; i < n; i++) {
        sum += data[i];
    }

    return sum;
}

// AVX版本（8路并行）
float sum_avx(const float* data, size_t n) {
    __m256 sum_vec = _mm256_setzero_ps();

    size_t i = 0;
    for (; i + 8 <= n; i += 8) {
        __m256 v = _mm256_loadu_ps(&data[i]);
        sum_vec = _mm256_add_ps(sum_vec, v);
    }

    // 水平求和
    __m128 sum_high = _mm256_extractf128_ps(sum_vec, 1);
    __m128 sum_low = _mm256_castps256_ps128(sum_vec);
    sum_low = _mm_add_ps(sum_low, sum_high);

    float sum = 0;
    sum += ((float*)&sum_low)[0];
    sum += ((float*)&sum_low)[1];
    sum += ((float*)&sum_low)[2];
    sum += ((float*)&sum_low)[3];

    for (; i < n; i++) {
        sum += data[i];
    }

    return sum;
}
```

**性能对比：**

```bash
# 编译不同版本
g++ -O3 -mno-sse -mno-avx sum.cpp -o sum_scalar
g++ -O3 -msse4.2 sum.cpp -o sum_sse
g++ -O3 -mavx2 sum.cpp -o sum_avx

# 测试（1亿个float）
./sum_scalar  # 250 ms
./sum_sse     # 65 ms (3.8x)
./sum_avx     # 35 ms (7.1x)
```

---

## 8. 编译器内建函数

### 8.1 常用内建函数

```cpp
// 1. 位操作
int popcount = __builtin_popcount(x);     // 计数1的个数
int clz = __builtin_clz(x);               // 前导0个数
int ctz = __builtin_ctz(x);               // 后尾0个数
int parity = __builtin_parity(x);         // 奇偶校验

// 2. 预取
__builtin_prefetch(ptr, 0, 3);  // 预取到L1 cache
// 参数2：0=读，1=写
// 参数3：0-3，cache级别

// 3. 预期值
if (__builtin_expect(x > 0, 1)) {  // x>0很可能为真
    // hot path
}

// 4. 数学函数
int abs = __builtin_abs(x);
long labs = __builtin_labs(x);
double fabs = __builtin_fabs(x);

// 5. 内存操作
__builtin_memcpy(dest, src, n);    // 可能内联
__builtin_memset(ptr, val, n);     // 可能内联
__builtin_memmove(dest, src, n);

// 6. 原子操作
int old = __sync_fetch_and_add(&var, 1);     // 原子加
bool success = __sync_bool_compare_and_swap(&var, old, new);

// 7. 栈操作
void* frame = __builtin_frame_address(0);  // 当前帧地址
void* ret = __builtin_return_address(0);   // 返回地址

// 8. 类型信息
size_t size = __builtin_object_size(ptr, 0);  // 对象大小
```

**内建函数性能：**

```cpp
#include <iostream>
#include <chrono>
#include <cstring>

void benchmark_builtin() {
    constexpr int N = 100000000;

    // 测试popcount
    auto start = std::chrono::high_resolution_clock::now();
    int sum = 0;
    for (int i = 0; i < N; i++) {
        // 标准方法
        int x = i;
        int count = 0;
        while (x) {
            count += x & 1;
            x >>= 1;
        }
        sum += count;
    }
    auto end = std::chrono::high_resolution_clock::now();
    auto std_time = std::chrono::duration_cast<std::chrono::milliseconds>(
        end - start).count();

    // 测试__builtin_popcount
    start = std::chrono::high_resolution_clock::now();
    sum = 0;
    for (int i = 0; i < N; i++) {
        sum += __builtin_popcount(i);  // POPCNT指令
    }
    end = std::chrono::high_resolution_clock::now();
    auto builtin_time = std::chrono::duration_cast<std::chrono::milliseconds>(
        end - start).count();

    std::cout << "Standard:  " << std_time << " ms\n";
    std::cout << "Builtin:   " << builtin_time << " ms\n";
    std::cout << "Speedup:   " << (double)std_time / builtin_time << "x\n";
}

// 输出：
// Standard:  3500 ms
// Builtin:   180 ms
// Speedup:   19.4x
```

---

## 总结

### 编译器优化最佳实践

```bash
# 推荐的生产编译配置
g++ -O3 \
    -march=native \
    -mtune=native \
    -flto \
    -fno-exceptions \           # 如果不需要异常
    -fno-rtti \                 # 如果不需要RTTI
    -DNDEBUG \                  # 禁用assert
    main.cpp -o app

# 性能关键代码的PGO
g++ -fprofile-generate -O3 main.cpp -o app_instr
./app_instr < workload.txt
g++ -fprofile-use -O3 main.cpp -o app_pgo

# 多目标编译（函数多版本）
g++ -O3 main.cpp -o app \
    -march=x86-64 \
    -Wa,-march=x86-64+avx2  # AVX2版本
```

### 性能提升总结

| 优化技术 | 典型提升 | 适用场景 |
|---------|---------|---------|
| -O2 → -O3 | 5-15% | 通用 |
| LTO | 10-30% | 大型项目 |
| PGO | 10-40% | 明确工作负载 |
| -march=native | 20-200% | SIMD密集型 |
| likely/unlikely | 5-30% | 偏斜分支 |
| 无分支代码 | 50-300% | 不可预测分支 |
| 函数内联 | 200-500% | 小函数 |

### 工具箱

```bash
# 查看编译器优化决策
g++ -O3 -fopt-info-vec-optimized main.cpp
g++ -O3 -Rpass=inline main.cpp

# 查看生成的汇编
g++ -O3 -S main.cpp -o main.s

# 性能分析
perf stat -e branches,branch-misses ./app

# 查看二进制信息
objdump -d app | less
nm -C app | grep ' T '  # 查看符号表
```

恭喜完成编译器优化技巧课程！下一课将学习CPU微架构优化。🎉
